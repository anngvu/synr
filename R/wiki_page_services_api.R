#' Synapse REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title WikiPageServices operations
#' @description WikiPageServicesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ deleteRepoV1EntityOwnerIdWikiWikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#'
#'
#' \item status code : 200 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ deleteRepoV1EvaluationOwnerIdWikiWikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#'
#'
#' \item status code : 200 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1AccessRequirementOwnerIdWikiWikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelWikiWikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelWikiWikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1AccessRequirementOwnerIdWikikey } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDaoWikiPageKey} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDaoWikiPageKey
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EntityOwnerIdWiki } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelWikiWikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelWikiWikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EntityOwnerIdWikiWikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelWikiWikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelWikiWikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EntityOwnerIdWikiWikiIdAttachment } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } file_name character
#' \item \emph{ @param } redirect character
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EntityOwnerIdWikiWikiIdAttachmenthandles } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelFileFileHandleResults} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelFileFileHandleResults
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EntityOwnerIdWikiWikiIdAttachmentpreview } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } file_name character
#' \item \emph{ @param } redirect character
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EntityOwnerIdWikiheadertree } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfWikiHeader} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfWikiHeader
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EntityOwnerIdWikikey } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDaoWikiPageKey} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDaoWikiPageKey
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationOwnerIdWiki } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelWikiWikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelWikiWikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationOwnerIdWikiWikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelWikiWikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelWikiWikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationOwnerIdWikiWikiIdAttachment } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } file_name character
#' \item \emph{ @param } redirect character
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationOwnerIdWikiWikiIdAttachmenthandles } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelFileFileHandleResults} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelFileFileHandleResults
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationOwnerIdWikiWikiIdAttachmentpreview } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } file_name character
#' \item \emph{ @param } redirect character
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationOwnerIdWikiheadertree } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfWikiHeader} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfWikiHeader
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationOwnerIdWikikey } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDaoWikiPageKey} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDaoWikiPageKey
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1AccessRequirementOwnerIdWiki } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_wiki_wiki_page \link{OrgSagebionetworksRepoModelWikiWikiPage}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelWikiWikiPage} \cr
#'
#'
#' \item status code : 201 | -
#'
#' \item return type : OrgSagebionetworksRepoModelWikiWikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1EntityOwnerIdWiki } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_wiki_wiki_page \link{OrgSagebionetworksRepoModelWikiWikiPage}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelWikiWikiPage} \cr
#'
#'
#' \item status code : 201 | -
#'
#' \item return type : OrgSagebionetworksRepoModelWikiWikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1EvaluationOwnerIdWiki } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_wiki_wiki_page \link{OrgSagebionetworksRepoModelWikiWikiPage}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelWikiWikiPage} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelWikiWikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1AccessRequirementOwnerIdWikiWikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_wiki_wiki_page \link{OrgSagebionetworksRepoModelWikiWikiPage}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelWikiWikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelWikiWikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1EntityOwnerIdWikiWikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_wiki_wiki_page \link{OrgSagebionetworksRepoModelWikiWikiPage}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelWikiWikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelWikiWikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1EvaluationOwnerIdWikiWikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_wiki_wiki_page \link{OrgSagebionetworksRepoModelWikiWikiPage}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelWikiWikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelWikiWikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  deleteRepoV1EntityOwnerIdWikiWikiId  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to delete.
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$deleteRepoV1EntityOwnerIdWikiWikiId(var_owner_id, var_wiki_id)
#'
#'
#' ####################  deleteRepoV1EvaluationOwnerIdWikiWikiId  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluations.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to delete.
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$deleteRepoV1EvaluationOwnerIdWikiWikiId(var_owner_id, var_wiki_id)
#'
#'
#' ####################  getRepoV1AccessRequirementOwnerIdWikiWikiId  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to get.
#' var_wiki_version <- 3.4 # numeric | When included returns a specific version of a wiki. (Optional)
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1AccessRequirementOwnerIdWikiWikiId(var_owner_id, var_wiki_id, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$getRepoV1AccessRequirementOwnerIdWikiWikiId(var_owner_id, var_wiki_id, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  getRepoV1AccessRequirementOwnerIdWikikey  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Access Requirement.
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1AccessRequirementOwnerIdWikikey(var_owner_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1AccessRequirementOwnerIdWikikey(var_owner_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1EntityOwnerIdWiki  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EntityOwnerIdWiki(var_owner_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1EntityOwnerIdWiki(var_owner_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1EntityOwnerIdWikiWikiId  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to get.
#' var_wiki_version <- 3.4 # numeric | When included returns a specific version of a wiki. (Optional)
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EntityOwnerIdWikiWikiId(var_owner_id, var_wiki_id, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$getRepoV1EntityOwnerIdWikiWikiId(var_owner_id, var_wiki_id, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  getRepoV1EntityOwnerIdWikiWikiIdAttachment  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage
#' var_file_name <- "file_name_example" # character | The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /entity/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
#' var_redirect <- "redirect_example" # character | When set to false, the URL will be returned as text/plain             instead of redirecting. (Optional)
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EntityOwnerIdWikiWikiIdAttachment(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirectdata_file = "result.txt")
#' result <- api_instance$getRepoV1EntityOwnerIdWikiWikiIdAttachment(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect)
#' dput(result)
#'
#'
#' ####################  getRepoV1EntityOwnerIdWikiWikiIdAttachmenthandles  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage.
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EntityOwnerIdWikiWikiIdAttachmenthandles(var_owner_id, var_wiki_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1EntityOwnerIdWikiWikiIdAttachmenthandles(var_owner_id, var_wiki_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1EntityOwnerIdWikiWikiIdAttachmentpreview  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage
#' var_file_name <- "file_name_example" # character | The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /entity/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
#' var_redirect <- "redirect_example" # character | When set to false, the URL will be returned as text/plain             instead of redirecting. (Optional)
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EntityOwnerIdWikiWikiIdAttachmentpreview(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirectdata_file = "result.txt")
#' result <- api_instance$getRepoV1EntityOwnerIdWikiWikiIdAttachmentpreview(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect)
#' dput(result)
#'
#'
#' ####################  getRepoV1EntityOwnerIdWikiheadertree  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#' var_offset <- 3.4 # numeric | The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10. (Optional)
#' var_limit <- 3.4 # numeric | Limits the size of the page returned. For example, a page size             of 10 require limit = 10. (Optional)
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EntityOwnerIdWikiheadertree(var_owner_id, offset = var_offset, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$getRepoV1EntityOwnerIdWikiheadertree(var_owner_id, offset = var_offset, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  getRepoV1EntityOwnerIdWikikey  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EntityOwnerIdWikikey(var_owner_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1EntityOwnerIdWikikey(var_owner_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationOwnerIdWiki  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluation.
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationOwnerIdWiki(var_owner_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationOwnerIdWiki(var_owner_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationOwnerIdWikiWikiId  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluation.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to get.
#' var_wiki_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationOwnerIdWikiWikiId(var_owner_id, var_wiki_id, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationOwnerIdWikiWikiId(var_owner_id, var_wiki_id, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationOwnerIdWikiWikiIdAttachment  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluation
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage
#' var_file_name <- "file_name_example" # character | The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.evaluation.ownerId.wiki.wikiId.attachmenthandles}\">,GET /evaluation/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>,             method.
#' var_redirect <- "redirect_example" # character | When set to false, the URL will be returned as text/plain             instead of redirecting. (Optional)
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationOwnerIdWikiWikiIdAttachment(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirectdata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationOwnerIdWikiWikiIdAttachment(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationOwnerIdWikiWikiIdAttachmenthandles  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluation.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage.
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationOwnerIdWikiWikiIdAttachmenthandles(var_owner_id, var_wiki_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationOwnerIdWikiWikiIdAttachmenthandles(var_owner_id, var_wiki_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationOwnerIdWikiWikiIdAttachmentpreview  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluation
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage
#' var_file_name <- "file_name_example" # character | The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.evaluation.ownerId.wiki.wikiId.attachmenthandles}\">,GET /evaluation/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>,             method.
#' var_redirect <- "redirect_example" # character | When set to false, the URL will be returned as text/plain             instead of redirecting. (Optional)
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationOwnerIdWikiWikiIdAttachmentpreview(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirectdata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationOwnerIdWikiWikiIdAttachmentpreview(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationOwnerIdWikiheadertree  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluation.
#' var_offset <- 3.4 # numeric | The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10. (Optional)
#' var_limit <- 3.4 # numeric | Limits the size of the page returned. For example, a page size             of 10 require limit = 10. (Optional)
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationOwnerIdWikiheadertree(var_owner_id, offset = var_offset, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationOwnerIdWikiheadertree(var_owner_id, offset = var_offset, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationOwnerIdWikikey  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluation.
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationOwnerIdWikikey(var_owner_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationOwnerIdWikikey(var_owner_id)
#' dput(result)
#'
#'
#' ####################  postRepoV1AccessRequirementOwnerIdWiki  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owner Entity.
#' var_org_sagebionetworks_repo_model_wiki_wiki_page <- org.sagebionetworks.repo.model.wiki.WikiPage$new("id_example", "etag_example", "createdOn_example", "createdBy_example", "modifiedOn_example", "modifiedBy_example", "parentWikiId_example", "title_example", "markdown_example", c("attachmentFileHandleIds_example")) # OrgSagebionetworksRepoModelWikiWikiPage | 
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1AccessRequirementOwnerIdWiki(var_owner_id, var_org_sagebionetworks_repo_model_wiki_wiki_pagedata_file = "result.txt")
#' result <- api_instance$postRepoV1AccessRequirementOwnerIdWiki(var_owner_id, var_org_sagebionetworks_repo_model_wiki_wiki_page)
#' dput(result)
#'
#'
#' ####################  postRepoV1EntityOwnerIdWiki  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owner Entity.
#' var_org_sagebionetworks_repo_model_wiki_wiki_page <- org.sagebionetworks.repo.model.wiki.WikiPage$new("id_example", "etag_example", "createdOn_example", "createdBy_example", "modifiedOn_example", "modifiedBy_example", "parentWikiId_example", "title_example", "markdown_example", c("attachmentFileHandleIds_example")) # OrgSagebionetworksRepoModelWikiWikiPage | 
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1EntityOwnerIdWiki(var_owner_id, var_org_sagebionetworks_repo_model_wiki_wiki_pagedata_file = "result.txt")
#' result <- api_instance$postRepoV1EntityOwnerIdWiki(var_owner_id, var_org_sagebionetworks_repo_model_wiki_wiki_page)
#' dput(result)
#'
#'
#' ####################  postRepoV1EvaluationOwnerIdWiki  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owner Evaluation.
#' var_org_sagebionetworks_repo_model_wiki_wiki_page <- org.sagebionetworks.repo.model.wiki.WikiPage$new("id_example", "etag_example", "createdOn_example", "createdBy_example", "modifiedOn_example", "modifiedBy_example", "parentWikiId_example", "title_example", "markdown_example", c("attachmentFileHandleIds_example")) # OrgSagebionetworksRepoModelWikiWikiPage | 
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1EvaluationOwnerIdWiki(var_owner_id, var_org_sagebionetworks_repo_model_wiki_wiki_pagedata_file = "result.txt")
#' result <- api_instance$postRepoV1EvaluationOwnerIdWiki(var_owner_id, var_org_sagebionetworks_repo_model_wiki_wiki_page)
#' dput(result)
#'
#'
#' ####################  putRepoV1AccessRequirementOwnerIdWikiWikiId  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to update.
#' var_org_sagebionetworks_repo_model_wiki_wiki_page <- org.sagebionetworks.repo.model.wiki.WikiPage$new("id_example", "etag_example", "createdOn_example", "createdBy_example", "modifiedOn_example", "modifiedBy_example", "parentWikiId_example", "title_example", "markdown_example", c("attachmentFileHandleIds_example")) # OrgSagebionetworksRepoModelWikiWikiPage | 
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$putRepoV1AccessRequirementOwnerIdWikiWikiId(var_owner_id, var_wiki_id, var_org_sagebionetworks_repo_model_wiki_wiki_pagedata_file = "result.txt")
#' result <- api_instance$putRepoV1AccessRequirementOwnerIdWikiWikiId(var_owner_id, var_wiki_id, var_org_sagebionetworks_repo_model_wiki_wiki_page)
#' dput(result)
#'
#'
#' ####################  putRepoV1EntityOwnerIdWikiWikiId  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to update.
#' var_org_sagebionetworks_repo_model_wiki_wiki_page <- org.sagebionetworks.repo.model.wiki.WikiPage$new("id_example", "etag_example", "createdOn_example", "createdBy_example", "modifiedOn_example", "modifiedBy_example", "parentWikiId_example", "title_example", "markdown_example", c("attachmentFileHandleIds_example")) # OrgSagebionetworksRepoModelWikiWikiPage | 
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$putRepoV1EntityOwnerIdWikiWikiId(var_owner_id, var_wiki_id, var_org_sagebionetworks_repo_model_wiki_wiki_pagedata_file = "result.txt")
#' result <- api_instance$putRepoV1EntityOwnerIdWikiWikiId(var_owner_id, var_wiki_id, var_org_sagebionetworks_repo_model_wiki_wiki_page)
#' dput(result)
#'
#'
#' ####################  putRepoV1EvaluationOwnerIdWikiWikiId  ####################
#'
#' library(synr)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluation.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to update.
#' var_org_sagebionetworks_repo_model_wiki_wiki_page <- org.sagebionetworks.repo.model.wiki.WikiPage$new("id_example", "etag_example", "createdOn_example", "createdBy_example", "modifiedOn_example", "modifiedBy_example", "parentWikiId_example", "title_example", "markdown_example", c("attachmentFileHandleIds_example")) # OrgSagebionetworksRepoModelWikiWikiPage | 
#'
#' api_instance <- WikiPageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$putRepoV1EvaluationOwnerIdWikiWikiId(var_owner_id, var_wiki_id, var_org_sagebionetworks_repo_model_wiki_wiki_pagedata_file = "result.txt")
#' result <- api_instance$putRepoV1EvaluationOwnerIdWikiWikiId(var_owner_id, var_wiki_id, var_org_sagebionetworks_repo_model_wiki_wiki_page)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
WikiPageServicesApi <- R6::R6Class(
  "WikiPageServicesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new WikiPageServicesApi.
    #'
    #' @description
    #' Initialize a new WikiPageServicesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to delete.
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    deleteRepoV1EntityOwnerIdWikiWikiId = function(owner_id, wiki_id, ...) {
      local_var_response <- self$deleteRepoV1EntityOwnerIdWikiWikiIdWithHttpInfo(owner_id, wiki_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to delete.
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    deleteRepoV1EntityOwnerIdWikiWikiIdWithHttpInfo = function(owner_id, wiki_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }



      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluations.
    #' @param wiki_id The ID of the WikiPage to delete.
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    deleteRepoV1EvaluationOwnerIdWikiWikiId = function(owner_id, wiki_id, ...) {
      local_var_response <- self$deleteRepoV1EvaluationOwnerIdWikiWikiIdWithHttpInfo(owner_id, wiki_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluations.
    #' @param wiki_id The ID of the WikiPage to delete.
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    deleteRepoV1EvaluationOwnerIdWikiWikiIdWithHttpInfo = function(owner_id, wiki_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }



      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to get.
    #' @param wiki_version (optional) When included returns a specific version of a wiki.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelWikiWikiPage
    #' @export
    getRepoV1AccessRequirementOwnerIdWikiWikiId = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1AccessRequirementOwnerIdWikiWikiIdWithHttpInfo(owner_id, wiki_id, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to get.
    #' @param wiki_version (optional) When included returns a specific version of a wiki.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelWikiWikiPage) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1AccessRequirementOwnerIdWikiWikiIdWithHttpInfo = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }




      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wiki/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelWikiWikiPage", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDaoWikiPageKey
    #' @export
    getRepoV1AccessRequirementOwnerIdWikikey = function(owner_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1AccessRequirementOwnerIdWikikeyWithHttpInfo(owner_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDaoWikiPageKey) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1AccessRequirementOwnerIdWikikeyWithHttpInfo = function(owner_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }


      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wikikey"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDaoWikiPageKey", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelWikiWikiPage
    #' @export
    getRepoV1EntityOwnerIdWiki = function(owner_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EntityOwnerIdWikiWithHttpInfo(owner_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelWikiWikiPage) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EntityOwnerIdWikiWithHttpInfo = function(owner_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }


      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelWikiWikiPage", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to get.
    #' @param wiki_version (optional) When included returns a specific version of a wiki.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelWikiWikiPage
    #' @export
    getRepoV1EntityOwnerIdWikiWikiId = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EntityOwnerIdWikiWikiIdWithHttpInfo(owner_id, wiki_id, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to get.
    #' @param wiki_version (optional) When included returns a specific version of a wiki.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelWikiWikiPage) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EntityOwnerIdWikiWikiIdWithHttpInfo = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }




      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelWikiWikiPage", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /entity/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    getRepoV1EntityOwnerIdWikiWikiIdAttachment = function(owner_id, wiki_id, file_name, redirect = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EntityOwnerIdWikiWikiIdAttachmentWithHttpInfo(owner_id, wiki_id, file_name, redirect, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /entity/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EntityOwnerIdWikiWikiIdAttachmentWithHttpInfo = function(owner_id, wiki_id, file_name, redirect = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`file_name`)) {
        stop("Missing required parameter `file_name`.")
      }





      query_params[["fileName"]] <- `file_name`

      query_params[["redirect"]] <- `redirect`

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki/{wikiId}/attachment"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelFileFileHandleResults
    #' @export
    getRepoV1EntityOwnerIdWikiWikiIdAttachmenthandles = function(owner_id, wiki_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EntityOwnerIdWikiWikiIdAttachmenthandlesWithHttpInfo(owner_id, wiki_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelFileFileHandleResults) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EntityOwnerIdWikiWikiIdAttachmenthandlesWithHttpInfo = function(owner_id, wiki_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }



      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki/{wikiId}/attachmenthandles"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelFileFileHandleResults", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /entity/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    getRepoV1EntityOwnerIdWikiWikiIdAttachmentpreview = function(owner_id, wiki_id, file_name, redirect = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EntityOwnerIdWikiWikiIdAttachmentpreviewWithHttpInfo(owner_id, wiki_id, file_name, redirect, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /entity/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EntityOwnerIdWikiWikiIdAttachmentpreviewWithHttpInfo = function(owner_id, wiki_id, file_name, redirect = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`file_name`)) {
        stop("Missing required parameter `file_name`.")
      }





      query_params[["fileName"]] <- `file_name`

      query_params[["redirect"]] <- `redirect`

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki/{wikiId}/attachmentpreview"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param offset (optional) The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit (optional) Limits the size of the page returned. For example, a page size             of 10 require limit = 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfWikiHeader
    #' @export
    getRepoV1EntityOwnerIdWikiheadertree = function(owner_id, offset = NULL, limit = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EntityOwnerIdWikiheadertreeWithHttpInfo(owner_id, offset, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param offset (optional) The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit (optional) Limits the size of the page returned. For example, a page size             of 10 require limit = 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfWikiHeader) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EntityOwnerIdWikiheadertreeWithHttpInfo = function(owner_id, offset = NULL, limit = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }




      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wikiheadertree"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfWikiHeader", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDaoWikiPageKey
    #' @export
    getRepoV1EntityOwnerIdWikikey = function(owner_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EntityOwnerIdWikikeyWithHttpInfo(owner_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDaoWikiPageKey) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EntityOwnerIdWikikeyWithHttpInfo = function(owner_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }


      local_var_url_path <- "/repo/v1/entity/{ownerId}/wikikey"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDaoWikiPageKey", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelWikiWikiPage
    #' @export
    getRepoV1EvaluationOwnerIdWiki = function(owner_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationOwnerIdWikiWithHttpInfo(owner_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelWikiWikiPage) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationOwnerIdWikiWithHttpInfo = function(owner_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }


      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelWikiWikiPage", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param wiki_id The ID of the WikiPage to get.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelWikiWikiPage
    #' @export
    getRepoV1EvaluationOwnerIdWikiWikiId = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationOwnerIdWikiWikiIdWithHttpInfo(owner_id, wiki_id, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param wiki_id The ID of the WikiPage to get.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelWikiWikiPage) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationOwnerIdWikiWikiIdWithHttpInfo = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }




      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelWikiWikiPage", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.evaluation.ownerId.wiki.wikiId.attachmenthandles}\">,GET /evaluation/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>,             method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    getRepoV1EvaluationOwnerIdWikiWikiIdAttachment = function(owner_id, wiki_id, file_name, redirect = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationOwnerIdWikiWikiIdAttachmentWithHttpInfo(owner_id, wiki_id, file_name, redirect, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.evaluation.ownerId.wiki.wikiId.attachmenthandles}\">,GET /evaluation/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>,             method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationOwnerIdWikiWikiIdAttachmentWithHttpInfo = function(owner_id, wiki_id, file_name, redirect = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`file_name`)) {
        stop("Missing required parameter `file_name`.")
      }





      query_params[["fileName"]] <- `file_name`

      query_params[["redirect"]] <- `redirect`

      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki/{wikiId}/attachment"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param wiki_id The ID of the WikiPage.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelFileFileHandleResults
    #' @export
    getRepoV1EvaluationOwnerIdWikiWikiIdAttachmenthandles = function(owner_id, wiki_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationOwnerIdWikiWikiIdAttachmenthandlesWithHttpInfo(owner_id, wiki_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param wiki_id The ID of the WikiPage.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelFileFileHandleResults) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationOwnerIdWikiWikiIdAttachmenthandlesWithHttpInfo = function(owner_id, wiki_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }



      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki/{wikiId}/attachmenthandles"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelFileFileHandleResults", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.evaluation.ownerId.wiki.wikiId.attachmenthandles}\">,GET /evaluation/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>,             method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    getRepoV1EvaluationOwnerIdWikiWikiIdAttachmentpreview = function(owner_id, wiki_id, file_name, redirect = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationOwnerIdWikiWikiIdAttachmentpreviewWithHttpInfo(owner_id, wiki_id, file_name, redirect, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.evaluation.ownerId.wiki.wikiId.attachmenthandles}\">,GET /evaluation/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>,             method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationOwnerIdWikiWikiIdAttachmentpreviewWithHttpInfo = function(owner_id, wiki_id, file_name, redirect = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`file_name`)) {
        stop("Missing required parameter `file_name`.")
      }





      query_params[["fileName"]] <- `file_name`

      query_params[["redirect"]] <- `redirect`

      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki/{wikiId}/attachmentpreview"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param offset (optional) The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit (optional) Limits the size of the page returned. For example, a page size             of 10 require limit = 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfWikiHeader
    #' @export
    getRepoV1EvaluationOwnerIdWikiheadertree = function(owner_id, offset = NULL, limit = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationOwnerIdWikiheadertreeWithHttpInfo(owner_id, offset, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param offset (optional) The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit (optional) Limits the size of the page returned. For example, a page size             of 10 require limit = 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfWikiHeader) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationOwnerIdWikiheadertreeWithHttpInfo = function(owner_id, offset = NULL, limit = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }




      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wikiheadertree"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfWikiHeader", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDaoWikiPageKey
    #' @export
    getRepoV1EvaluationOwnerIdWikikey = function(owner_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationOwnerIdWikikeyWithHttpInfo(owner_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDaoWikiPageKey) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationOwnerIdWikikeyWithHttpInfo = function(owner_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }


      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wikikey"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDaoWikiPageKey", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owner Entity.
    #' @param org_sagebionetworks_repo_model_wiki_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelWikiWikiPage
    #' @export
    postRepoV1AccessRequirementOwnerIdWiki = function(owner_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1AccessRequirementOwnerIdWikiWithHttpInfo(owner_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owner Entity.
    #' @param org_sagebionetworks_repo_model_wiki_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelWikiWikiPage) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1AccessRequirementOwnerIdWikiWithHttpInfo = function(owner_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_wiki_wiki_page`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_wiki_wiki_page`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_wiki_wiki_page`)) {
        local_var_body <- `org_sagebionetworks_repo_model_wiki_wiki_page`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wiki"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelWikiWikiPage", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owner Entity.
    #' @param org_sagebionetworks_repo_model_wiki_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelWikiWikiPage
    #' @export
    postRepoV1EntityOwnerIdWiki = function(owner_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1EntityOwnerIdWikiWithHttpInfo(owner_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owner Entity.
    #' @param org_sagebionetworks_repo_model_wiki_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelWikiWikiPage) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1EntityOwnerIdWikiWithHttpInfo = function(owner_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_wiki_wiki_page`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_wiki_wiki_page`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_wiki_wiki_page`)) {
        local_var_body <- `org_sagebionetworks_repo_model_wiki_wiki_page`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelWikiWikiPage", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owner Evaluation.
    #' @param org_sagebionetworks_repo_model_wiki_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelWikiWikiPage
    #' @export
    postRepoV1EvaluationOwnerIdWiki = function(owner_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1EvaluationOwnerIdWikiWithHttpInfo(owner_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owner Evaluation.
    #' @param org_sagebionetworks_repo_model_wiki_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelWikiWikiPage) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1EvaluationOwnerIdWikiWithHttpInfo = function(owner_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_wiki_wiki_page`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_wiki_wiki_page`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_wiki_wiki_page`)) {
        local_var_body <- `org_sagebionetworks_repo_model_wiki_wiki_page`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelWikiWikiPage", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param org_sagebionetworks_repo_model_wiki_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelWikiWikiPage
    #' @export
    putRepoV1AccessRequirementOwnerIdWikiWikiId = function(owner_id, wiki_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = NULL, ...) {
      local_var_response <- self$putRepoV1AccessRequirementOwnerIdWikiWikiIdWithHttpInfo(owner_id, wiki_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param org_sagebionetworks_repo_model_wiki_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelWikiWikiPage) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1AccessRequirementOwnerIdWikiWikiIdWithHttpInfo = function(owner_id, wiki_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_wiki_wiki_page`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_wiki_wiki_page`.")
      }




      if (!is.null(`org_sagebionetworks_repo_model_wiki_wiki_page`)) {
        local_var_body <- `org_sagebionetworks_repo_model_wiki_wiki_page`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wiki/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelWikiWikiPage", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param org_sagebionetworks_repo_model_wiki_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelWikiWikiPage
    #' @export
    putRepoV1EntityOwnerIdWikiWikiId = function(owner_id, wiki_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = NULL, ...) {
      local_var_response <- self$putRepoV1EntityOwnerIdWikiWikiIdWithHttpInfo(owner_id, wiki_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param org_sagebionetworks_repo_model_wiki_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelWikiWikiPage) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1EntityOwnerIdWikiWikiIdWithHttpInfo = function(owner_id, wiki_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_wiki_wiki_page`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_wiki_wiki_page`.")
      }




      if (!is.null(`org_sagebionetworks_repo_model_wiki_wiki_page`)) {
        local_var_body <- `org_sagebionetworks_repo_model_wiki_wiki_page`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelWikiWikiPage", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param org_sagebionetworks_repo_model_wiki_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelWikiWikiPage
    #' @export
    putRepoV1EvaluationOwnerIdWikiWikiId = function(owner_id, wiki_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = NULL, ...) {
      local_var_response <- self$putRepoV1EvaluationOwnerIdWikiWikiIdWithHttpInfo(owner_id, wiki_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param org_sagebionetworks_repo_model_wiki_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelWikiWikiPage) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1EvaluationOwnerIdWikiWikiIdWithHttpInfo = function(owner_id, wiki_id, org_sagebionetworks_repo_model_wiki_wiki_page, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_wiki_wiki_page`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_wiki_wiki_page`.")
      }




      if (!is.null(`org_sagebionetworks_repo_model_wiki_wiki_page`)) {
        local_var_body <- `org_sagebionetworks_repo_model_wiki_wiki_page`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelWikiWikiPage", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
