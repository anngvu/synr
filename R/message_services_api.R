#' Synapse REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title MessageServices operations
#' @description MessageServicesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ deleteRepoV1AdminMessageMessageId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } message_id character
#'
#'
#' \item status code : 200 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1MessageInbox } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } inbox_filter character
#' \item \emph{ @param } order_by character
#' \item \emph{ @param } descending character
#' \item \emph{ @param } limit numeric
#' \item \emph{ @param } offset numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfMessageBundle} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfMessageBundle
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1MessageMessageId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } message_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelMessageMessageToUser} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelMessageMessageToUser
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1MessageMessageIdConversation } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } message_id character
#' \item \emph{ @param } order_by character
#' \item \emph{ @param } descending character
#' \item \emph{ @param } limit numeric
#' \item \emph{ @param } offset numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfMessageToUser} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfMessageToUser
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1MessageMessageIdFile } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } message_id character
#' \item \emph{ @param } redirect character
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1MessageOutbox } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } order_by character
#' \item \emph{ @param } descending character
#' \item \emph{ @param } limit numeric
#' \item \emph{ @param } offset numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfMessageToUser} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfMessageToUser
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1CloudMailInAuthorization } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_message_cloudmailin_authorization_check_header \link{OrgSagebionetworksRepoModelMessageCloudmailinAuthorizationCheckHeader}
#'
#'
#' \item status code : 204 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1CloudMailInMessage } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_message_cloudmailin_message \link{OrgSagebionetworksRepoModelMessageCloudmailinMessage}
#' \item \emph{ @param } notification_unsubscribe_endpoint character
#'
#'
#' \item status code : 204 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1EntityIdMessage } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_message_message_to_user \link{OrgSagebionetworksRepoModelMessageMessageToUser}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelMessageMessageToUser} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelMessageMessageToUser
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1Message } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_message_message_to_user \link{OrgSagebionetworksRepoModelMessageMessageToUser}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelMessageMessageToUser} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelMessageMessageToUser
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1MessageMessageIdForward } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } message_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_message_message_recipient_set \link{OrgSagebionetworksRepoModelMessageMessageRecipientSet}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelMessageMessageToUser} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelMessageMessageToUser
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1MessageStatus } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_message_message_status \link{OrgSagebionetworksRepoModelMessageMessageStatus}
#'
#'
#' \item status code : 200 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  deleteRepoV1AdminMessageMessageId  ####################
#'
#' library(synr)
#' var_message_id <- "message_id_example" # character | 
#'
#' api_instance <- MessageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$deleteRepoV1AdminMessageMessageId(var_message_id)
#'
#'
#' ####################  getRepoV1MessageInbox  ####################
#'
#' library(synr)
#' var_inbox_filter <- "inbox_filter_example" # character | 
#' var_order_by <- "order_by_example" # character | 
#' var_descending <- "descending_example" # character | 
#' var_limit <- 3.4 # numeric | 
#' var_offset <- 3.4 # numeric | 
#'
#' api_instance <- MessageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1MessageInbox(var_inbox_filter, var_order_by, var_descending, var_limit, var_offsetdata_file = "result.txt")
#' result <- api_instance$getRepoV1MessageInbox(var_inbox_filter, var_order_by, var_descending, var_limit, var_offset)
#' dput(result)
#'
#'
#' ####################  getRepoV1MessageMessageId  ####################
#'
#' library(synr)
#' var_message_id <- "message_id_example" # character | 
#'
#' api_instance <- MessageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1MessageMessageId(var_message_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1MessageMessageId(var_message_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1MessageMessageIdConversation  ####################
#'
#' library(synr)
#' var_message_id <- "message_id_example" # character | 
#' var_order_by <- "order_by_example" # character | 
#' var_descending <- "descending_example" # character | 
#' var_limit <- 3.4 # numeric | 
#' var_offset <- 3.4 # numeric | 
#'
#' api_instance <- MessageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1MessageMessageIdConversation(var_message_id, var_order_by, var_descending, var_limit, var_offsetdata_file = "result.txt")
#' result <- api_instance$getRepoV1MessageMessageIdConversation(var_message_id, var_order_by, var_descending, var_limit, var_offset)
#' dput(result)
#'
#'
#' ####################  getRepoV1MessageMessageIdFile  ####################
#'
#' library(synr)
#' var_message_id <- "message_id_example" # character | 
#' var_redirect <- "redirect_example" # character | When set to false, the URL will be returned as text/plain             instead of redirecting. (Optional)
#'
#' api_instance <- MessageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1MessageMessageIdFile(var_message_id, redirect = var_redirectdata_file = "result.txt")
#' result <- api_instance$getRepoV1MessageMessageIdFile(var_message_id, redirect = var_redirect)
#' dput(result)
#'
#'
#' ####################  getRepoV1MessageOutbox  ####################
#'
#' library(synr)
#' var_order_by <- "order_by_example" # character | 
#' var_descending <- "descending_example" # character | 
#' var_limit <- 3.4 # numeric | 
#' var_offset <- 3.4 # numeric | 
#'
#' api_instance <- MessageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1MessageOutbox(var_order_by, var_descending, var_limit, var_offsetdata_file = "result.txt")
#' result <- api_instance$getRepoV1MessageOutbox(var_order_by, var_descending, var_limit, var_offset)
#' dput(result)
#'
#'
#' ####################  postRepoV1CloudMailInAuthorization  ####################
#'
#' library(synr)
#' var_org_sagebionetworks_repo_model_message_cloudmailin_authorization_check_header <- org.sagebionetworks.repo.model.message.cloudmailin.AuthorizationCheckHeader$new("to_example", "from_example", 123, "helo_domain_example", "remote_ip_example") # OrgSagebionetworksRepoModelMessageCloudmailinAuthorizationCheckHeader | 
#'
#' api_instance <- MessageServicesApi$new()
#'
#' api_instance$postRepoV1CloudMailInAuthorization(var_org_sagebionetworks_repo_model_message_cloudmailin_authorization_check_header)
#'
#'
#' ####################  postRepoV1CloudMailInMessage  ####################
#'
#' library(synr)
#' var_org_sagebionetworks_repo_model_message_cloudmailin_message <- org.sagebionetworks.repo.model.message.cloudmailin.Message$new(org.sagebionetworks.repo.model.message.cloudmailin.Envelope$new("to_example", c("recipients_example"), "from_example", "helo_domain_example", "remote_ip_example", "spf_example"), org.sagebionetworks.repo.model.message.cloudmailin.Headers$new("To_example", "Cc_example", "Bcc_example", "Subject_example", "From_example"), "plain_example", "html_example", "reply_plain_example", c(org.sagebionetworks.repo.model.message.cloudmailin.Attachment$new("content_example", "content_id_example", "url_example", "file_name_example", "content_type_example", "size_example", "disposition_example"))) # OrgSagebionetworksRepoModelMessageCloudmailinMessage | 
#' var_notification_unsubscribe_endpoint <- "notification_unsubscribe_endpoint_example" # character |  (Optional)
#'
#' api_instance <- MessageServicesApi$new()
#'
#' api_instance$postRepoV1CloudMailInMessage(var_org_sagebionetworks_repo_model_message_cloudmailin_message, notification_unsubscribe_endpoint = var_notification_unsubscribe_endpoint)
#'
#'
#' ####################  postRepoV1EntityIdMessage  ####################
#'
#' library(synr)
#' var_id <- "id_example" # character | 
#' var_org_sagebionetworks_repo_model_message_message_to_user <- org.sagebionetworks.repo.model.message.MessageToUser$new("id_example", "createdBy_example", "fileHandleId_example", "createdOn_example", c("recipients_example"), "subject_example", "inReplyTo_example", "inReplyToRoot_example", "notificationUnsubscribeEndpoint_example", "userProfileSettingEndpoint_example", "withUnsubscribeLink_example", "withProfileSettingLink_example", "isNotificationMessage_example", "to_example", "cc_example", "bcc_example") # OrgSagebionetworksRepoModelMessageMessageToUser | 
#'
#' api_instance <- MessageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1EntityIdMessage(var_id, var_org_sagebionetworks_repo_model_message_message_to_userdata_file = "result.txt")
#' result <- api_instance$postRepoV1EntityIdMessage(var_id, var_org_sagebionetworks_repo_model_message_message_to_user)
#' dput(result)
#'
#'
#' ####################  postRepoV1Message  ####################
#'
#' library(synr)
#' var_org_sagebionetworks_repo_model_message_message_to_user <- org.sagebionetworks.repo.model.message.MessageToUser$new("id_example", "createdBy_example", "fileHandleId_example", "createdOn_example", c("recipients_example"), "subject_example", "inReplyTo_example", "inReplyToRoot_example", "notificationUnsubscribeEndpoint_example", "userProfileSettingEndpoint_example", "withUnsubscribeLink_example", "withProfileSettingLink_example", "isNotificationMessage_example", "to_example", "cc_example", "bcc_example") # OrgSagebionetworksRepoModelMessageMessageToUser | 
#'
#' api_instance <- MessageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1Message(var_org_sagebionetworks_repo_model_message_message_to_userdata_file = "result.txt")
#' result <- api_instance$postRepoV1Message(var_org_sagebionetworks_repo_model_message_message_to_user)
#' dput(result)
#'
#'
#' ####################  postRepoV1MessageMessageIdForward  ####################
#'
#' library(synr)
#' var_message_id <- "message_id_example" # character | 
#' var_org_sagebionetworks_repo_model_message_message_recipient_set <- org.sagebionetworks.repo.model.message.MessageRecipientSet$new(c("recipients_example")) # OrgSagebionetworksRepoModelMessageMessageRecipientSet | 
#'
#' api_instance <- MessageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1MessageMessageIdForward(var_message_id, var_org_sagebionetworks_repo_model_message_message_recipient_setdata_file = "result.txt")
#' result <- api_instance$postRepoV1MessageMessageIdForward(var_message_id, var_org_sagebionetworks_repo_model_message_message_recipient_set)
#' dput(result)
#'
#'
#' ####################  putRepoV1MessageStatus  ####################
#'
#' library(synr)
#' var_org_sagebionetworks_repo_model_message_message_status <- org.sagebionetworks.repo.model.message.MessageStatus$new("messageId_example", "recipientId_example", "status_example") # OrgSagebionetworksRepoModelMessageMessageStatus | 
#'
#' api_instance <- MessageServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$putRepoV1MessageStatus(var_org_sagebionetworks_repo_model_message_message_status)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
MessageServicesApi <- R6::R6Class(
  "MessageServicesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new MessageServicesApi.
    #'
    #' @description
    #' Initialize a new MessageServicesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param message_id 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    deleteRepoV1AdminMessageMessageId = function(message_id, ...) {
      local_var_response <- self$deleteRepoV1AdminMessageMessageIdWithHttpInfo(message_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param message_id 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    deleteRepoV1AdminMessageMessageIdWithHttpInfo = function(message_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`message_id`)) {
        stop("Missing required parameter `message_id`.")
      }


      local_var_url_path <- "/repo/v1/admin/message/{messageId}"
      if (!missing(`message_id`)) {
        local_var_url_path <- gsub("\\{messageId\\}", URLencode(as.character(`message_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param inbox_filter 
    #' @param order_by 
    #' @param descending 
    #' @param limit 
    #' @param offset 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfMessageBundle
    #' @export
    getRepoV1MessageInbox = function(inbox_filter, order_by, descending, limit, offset, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1MessageInboxWithHttpInfo(inbox_filter, order_by, descending, limit, offset, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param inbox_filter 
    #' @param order_by 
    #' @param descending 
    #' @param limit 
    #' @param offset 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfMessageBundle) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1MessageInboxWithHttpInfo = function(inbox_filter, order_by, descending, limit, offset, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`inbox_filter`)) {
        stop("Missing required parameter `inbox_filter`.")
      }

      if (missing(`order_by`)) {
        stop("Missing required parameter `order_by`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`offset`)) {
        stop("Missing required parameter `offset`.")
      }






      query_params[["inboxFilter"]] <- `inbox_filter`

      query_params[["orderBy"]] <- `order_by`

      query_params[["descending"]] <- `descending`

      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      local_var_url_path <- "/repo/v1/message/inbox"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfMessageBundle", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param message_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelMessageMessageToUser
    #' @export
    getRepoV1MessageMessageId = function(message_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1MessageMessageIdWithHttpInfo(message_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param message_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelMessageMessageToUser) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1MessageMessageIdWithHttpInfo = function(message_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`message_id`)) {
        stop("Missing required parameter `message_id`.")
      }


      local_var_url_path <- "/repo/v1/message/{messageId}"
      if (!missing(`message_id`)) {
        local_var_url_path <- gsub("\\{messageId\\}", URLencode(as.character(`message_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelMessageMessageToUser", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param message_id 
    #' @param order_by 
    #' @param descending 
    #' @param limit 
    #' @param offset 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfMessageToUser
    #' @export
    getRepoV1MessageMessageIdConversation = function(message_id, order_by, descending, limit, offset, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1MessageMessageIdConversationWithHttpInfo(message_id, order_by, descending, limit, offset, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param message_id 
    #' @param order_by 
    #' @param descending 
    #' @param limit 
    #' @param offset 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfMessageToUser) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1MessageMessageIdConversationWithHttpInfo = function(message_id, order_by, descending, limit, offset, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`message_id`)) {
        stop("Missing required parameter `message_id`.")
      }

      if (missing(`order_by`)) {
        stop("Missing required parameter `order_by`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`offset`)) {
        stop("Missing required parameter `offset`.")
      }






      query_params[["orderBy"]] <- `order_by`

      query_params[["descending"]] <- `descending`

      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      local_var_url_path <- "/repo/v1/message/{messageId}/conversation"
      if (!missing(`message_id`)) {
        local_var_url_path <- gsub("\\{messageId\\}", URLencode(as.character(`message_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfMessageToUser", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param message_id 
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    getRepoV1MessageMessageIdFile = function(message_id, redirect = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1MessageMessageIdFileWithHttpInfo(message_id, redirect, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param message_id 
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1MessageMessageIdFileWithHttpInfo = function(message_id, redirect = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`message_id`)) {
        stop("Missing required parameter `message_id`.")
      }



      query_params[["redirect"]] <- `redirect`

      local_var_url_path <- "/repo/v1/message/{messageId}/file"
      if (!missing(`message_id`)) {
        local_var_url_path <- gsub("\\{messageId\\}", URLencode(as.character(`message_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param order_by 
    #' @param descending 
    #' @param limit 
    #' @param offset 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfMessageToUser
    #' @export
    getRepoV1MessageOutbox = function(order_by, descending, limit, offset, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1MessageOutboxWithHttpInfo(order_by, descending, limit, offset, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param order_by 
    #' @param descending 
    #' @param limit 
    #' @param offset 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfMessageToUser) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1MessageOutboxWithHttpInfo = function(order_by, descending, limit, offset, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`order_by`)) {
        stop("Missing required parameter `order_by`.")
      }

      if (missing(`descending`)) {
        stop("Missing required parameter `descending`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`offset`)) {
        stop("Missing required parameter `offset`.")
      }





      query_params[["orderBy"]] <- `order_by`

      query_params[["descending"]] <- `descending`

      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      local_var_url_path <- "/repo/v1/message/outbox"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfMessageToUser", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_message_cloudmailin_authorization_check_header 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    postRepoV1CloudMailInAuthorization = function(org_sagebionetworks_repo_model_message_cloudmailin_authorization_check_header, ...) {
      local_var_response <- self$postRepoV1CloudMailInAuthorizationWithHttpInfo(org_sagebionetworks_repo_model_message_cloudmailin_authorization_check_header, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_message_cloudmailin_authorization_check_header 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1CloudMailInAuthorizationWithHttpInfo = function(org_sagebionetworks_repo_model_message_cloudmailin_authorization_check_header, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_message_cloudmailin_authorization_check_header`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_message_cloudmailin_authorization_check_header`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_message_cloudmailin_authorization_check_header`)) {
        local_var_body <- `org_sagebionetworks_repo_model_message_cloudmailin_authorization_check_header`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/cloudMailInAuthorization"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_message_cloudmailin_message 
    #' @param notification_unsubscribe_endpoint (optional) No description
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    postRepoV1CloudMailInMessage = function(org_sagebionetworks_repo_model_message_cloudmailin_message, notification_unsubscribe_endpoint = NULL, ...) {
      local_var_response <- self$postRepoV1CloudMailInMessageWithHttpInfo(org_sagebionetworks_repo_model_message_cloudmailin_message, notification_unsubscribe_endpoint, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_message_cloudmailin_message 
    #' @param notification_unsubscribe_endpoint (optional) No description
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1CloudMailInMessageWithHttpInfo = function(org_sagebionetworks_repo_model_message_cloudmailin_message, notification_unsubscribe_endpoint = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_message_cloudmailin_message`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_message_cloudmailin_message`.")
      }



      query_params[["notificationUnsubscribeEndpoint"]] <- `notification_unsubscribe_endpoint`

      if (!is.null(`org_sagebionetworks_repo_model_message_cloudmailin_message`)) {
        local_var_body <- `org_sagebionetworks_repo_model_message_cloudmailin_message`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/cloudMailInMessage"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param org_sagebionetworks_repo_model_message_message_to_user 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelMessageMessageToUser
    #' @export
    postRepoV1EntityIdMessage = function(id, org_sagebionetworks_repo_model_message_message_to_user, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1EntityIdMessageWithHttpInfo(id, org_sagebionetworks_repo_model_message_message_to_user, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param org_sagebionetworks_repo_model_message_message_to_user 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelMessageMessageToUser) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1EntityIdMessageWithHttpInfo = function(id, org_sagebionetworks_repo_model_message_message_to_user, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_message_message_to_user`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_message_message_to_user`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_message_message_to_user`)) {
        local_var_body <- `org_sagebionetworks_repo_model_message_message_to_user`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/entity/{id}/message"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelMessageMessageToUser", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_message_message_to_user 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelMessageMessageToUser
    #' @export
    postRepoV1Message = function(org_sagebionetworks_repo_model_message_message_to_user, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1MessageWithHttpInfo(org_sagebionetworks_repo_model_message_message_to_user, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_message_message_to_user 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelMessageMessageToUser) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1MessageWithHttpInfo = function(org_sagebionetworks_repo_model_message_message_to_user, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_message_message_to_user`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_message_message_to_user`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_message_message_to_user`)) {
        local_var_body <- `org_sagebionetworks_repo_model_message_message_to_user`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/message"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelMessageMessageToUser", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param message_id 
    #' @param org_sagebionetworks_repo_model_message_message_recipient_set 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelMessageMessageToUser
    #' @export
    postRepoV1MessageMessageIdForward = function(message_id, org_sagebionetworks_repo_model_message_message_recipient_set, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1MessageMessageIdForwardWithHttpInfo(message_id, org_sagebionetworks_repo_model_message_message_recipient_set, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param message_id 
    #' @param org_sagebionetworks_repo_model_message_message_recipient_set 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelMessageMessageToUser) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1MessageMessageIdForwardWithHttpInfo = function(message_id, org_sagebionetworks_repo_model_message_message_recipient_set, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`message_id`)) {
        stop("Missing required parameter `message_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_message_message_recipient_set`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_message_message_recipient_set`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_message_message_recipient_set`)) {
        local_var_body <- `org_sagebionetworks_repo_model_message_message_recipient_set`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/message/{messageId}/forward"
      if (!missing(`message_id`)) {
        local_var_url_path <- gsub("\\{messageId\\}", URLencode(as.character(`message_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelMessageMessageToUser", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_message_message_status 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    putRepoV1MessageStatus = function(org_sagebionetworks_repo_model_message_message_status, ...) {
      local_var_response <- self$putRepoV1MessageStatusWithHttpInfo(org_sagebionetworks_repo_model_message_message_status, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_message_message_status 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1MessageStatusWithHttpInfo = function(org_sagebionetworks_repo_model_message_message_status, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_message_message_status`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_message_message_status`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_message_message_status`)) {
        local_var_body <- `org_sagebionetworks_repo_model_message_message_status`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/message/status"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
