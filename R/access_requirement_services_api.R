#' Synapse REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title AccessRequirementServices operations
#' @description AccessRequirementServicesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ deleteRepoV1AccessRequirementRequirementId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } requirement_id character
#'
#'
#' \item status code : 200 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ deleteRepoV1AccessRequirementRequirementIdAcl } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } requirement_id character
#'
#'
#' \item status code : 200 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1AccessRequirementRequirementId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } requirement_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelAccessRequirement} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelAccessRequirement
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1AccessRequirementRequirementIdAcl } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } requirement_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelAccessControlList} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelAccessControlList
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1AccessRequirementRequirementIdSubjects } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } requirement_id character
#' \item \emph{ @param } next_page_token character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelRestrictableObjectDescriptorResponse} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelRestrictableObjectDescriptorResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EntityIdAccessRequirement } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @param } limit numeric
#' \item \emph{ @param } offset numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfAccessRequirement} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfAccessRequirement
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1TeamIdAccessRequirement } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @param } limit numeric
#' \item \emph{ @param } offset numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfAccessRequirement} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfAccessRequirement
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1AccessRequirement } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_access_requirement \link{OrgSagebionetworksRepoModelAccessRequirement}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelAccessRequirement} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelAccessRequirement
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1AccessRequirementRequirementIdAcl } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } requirement_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_access_control_list \link{OrgSagebionetworksRepoModelAccessControlList}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelAccessControlList} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelAccessControlList
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1AccessRequirementSearch } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_dataaccess_access_requirement_search_request \link{OrgSagebionetworksRepoModelDataaccessAccessRequirementSearchRequest}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDataaccessAccessRequirementSearchResponse} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDataaccessAccessRequirementSearchResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1EntityIdLockAccessRequirement } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelAccessRequirement} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelAccessRequirement
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1AccessRequirementConversion } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_dataaccess_access_requirement_conversion_request \link{OrgSagebionetworksRepoModelDataaccessAccessRequirementConversionRequest}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelAccessRequirement} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelAccessRequirement
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1AccessRequirementRequirementId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } requirement_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_access_requirement \link{OrgSagebionetworksRepoModelAccessRequirement}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelAccessRequirement} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelAccessRequirement
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1AccessRequirementRequirementIdAcl } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } requirement_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_access_control_list \link{OrgSagebionetworksRepoModelAccessControlList}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelAccessControlList} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelAccessControlList
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  deleteRepoV1AccessRequirementRequirementId  ####################
#'
#' library(synr)
#' var_requirement_id <- "requirement_id_example" # character | the ID of the requirement to delete
#'
#' api_instance <- AccessRequirementServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$deleteRepoV1AccessRequirementRequirementId(var_requirement_id)
#'
#'
#' ####################  deleteRepoV1AccessRequirementRequirementIdAcl  ####################
#'
#' library(synr)
#' var_requirement_id <- "requirement_id_example" # character | 
#'
#' api_instance <- AccessRequirementServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$deleteRepoV1AccessRequirementRequirementIdAcl(var_requirement_id)
#'
#'
#' ####################  getRepoV1AccessRequirementRequirementId  ####################
#'
#' library(synr)
#' var_requirement_id <- "requirement_id_example" # character | 
#'
#' api_instance <- AccessRequirementServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1AccessRequirementRequirementId(var_requirement_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1AccessRequirementRequirementId(var_requirement_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1AccessRequirementRequirementIdAcl  ####################
#'
#' library(synr)
#' var_requirement_id <- "requirement_id_example" # character | 
#'
#' api_instance <- AccessRequirementServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1AccessRequirementRequirementIdAcl(var_requirement_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1AccessRequirementRequirementIdAcl(var_requirement_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1AccessRequirementRequirementIdSubjects  ####################
#'
#' library(synr)
#' var_requirement_id <- "requirement_id_example" # character | 
#' var_next_page_token <- "next_page_token_example" # character |  (Optional)
#'
#' api_instance <- AccessRequirementServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1AccessRequirementRequirementIdSubjects(var_requirement_id, next_page_token = var_next_page_tokendata_file = "result.txt")
#' result <- api_instance$getRepoV1AccessRequirementRequirementIdSubjects(var_requirement_id, next_page_token = var_next_page_token)
#' dput(result)
#'
#'
#' ####################  getRepoV1EntityIdAccessRequirement  ####################
#'
#' library(synr)
#' var_id <- "id_example" # character | 
#' var_limit <- 3.4 # numeric | - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum limit for this call is 50. (Optional)
#' var_offset <- 3.4 # numeric | - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10. (Optional)
#'
#' api_instance <- AccessRequirementServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EntityIdAccessRequirement(var_id, limit = var_limit, offset = var_offsetdata_file = "result.txt")
#' result <- api_instance$getRepoV1EntityIdAccessRequirement(var_id, limit = var_limit, offset = var_offset)
#' dput(result)
#'
#'
#' ####################  getRepoV1TeamIdAccessRequirement  ####################
#'
#' library(synr)
#' var_id <- "id_example" # character | the ID of the Team whose Access Requirements are retrieved.
#' var_limit <- 3.4 # numeric | - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum limit for this call is 50. (Optional)
#' var_offset <- 3.4 # numeric | - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10. (Optional)
#'
#' api_instance <- AccessRequirementServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1TeamIdAccessRequirement(var_id, limit = var_limit, offset = var_offsetdata_file = "result.txt")
#' result <- api_instance$getRepoV1TeamIdAccessRequirement(var_id, limit = var_limit, offset = var_offset)
#' dput(result)
#'
#'
#' ####################  postRepoV1AccessRequirement  ####################
#'
#' library(synr)
#' var_org_sagebionetworks_repo_model_access_requirement <- org.sagebionetworks.repo.model.AccessRequirement$new("concreteType_example", 123, 123, "description_example", "name_example", "etag_example", "createdOn_example", "modifiedOn_example", "createdBy_example", "modifiedBy_example", "subjectsDefinedByAnnotations_example", c(org.sagebionetworks.repo.model.RestrictableObjectDescriptor$new("id_example", "type_example")), "accessType_example", "url_example", "termsOfUse_example", "isCertifiedUserRequired_example", "isValidatedProfileRequired_example", "isDUCRequired_example", "ducTemplateFileHandleId_example", "isIRBApprovalRequired_example", "areOtherAttachmentsRequired_example", 123, "isIDUPublic_example", "isIDURequired_example", "isTwoFaRequired_example", "actContactInfo_example", "openJiraIssue_example", "jiraKey_example") # OrgSagebionetworksRepoModelAccessRequirement | 
#'
#' api_instance <- AccessRequirementServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1AccessRequirement(var_org_sagebionetworks_repo_model_access_requirementdata_file = "result.txt")
#' result <- api_instance$postRepoV1AccessRequirement(var_org_sagebionetworks_repo_model_access_requirement)
#' dput(result)
#'
#'
#' ####################  postRepoV1AccessRequirementRequirementIdAcl  ####################
#'
#' library(synr)
#' var_requirement_id <- "requirement_id_example" # character | 
#' var_org_sagebionetworks_repo_model_access_control_list <- org.sagebionetworks.repo.model.AccessControlList$new("id_example", "createdBy_example", "creationDate_example", "modifiedBy_example", "modifiedOn_example", "etag_example", c(org.sagebionetworks.repo.model.ResourceAccess$new(123, c("accessType_example")))) # OrgSagebionetworksRepoModelAccessControlList | 
#'
#' api_instance <- AccessRequirementServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1AccessRequirementRequirementIdAcl(var_requirement_id, var_org_sagebionetworks_repo_model_access_control_listdata_file = "result.txt")
#' result <- api_instance$postRepoV1AccessRequirementRequirementIdAcl(var_requirement_id, var_org_sagebionetworks_repo_model_access_control_list)
#' dput(result)
#'
#'
#' ####################  postRepoV1AccessRequirementSearch  ####################
#'
#' library(synr)
#' var_org_sagebionetworks_repo_model_dataaccess_access_requirement_search_request <- org.sagebionetworks.repo.model.dataaccess.AccessRequirementSearchRequest$new("nameContains_example", c(123), "relatedProjectId_example", "reviewerId_example", "accessType_example", "type_example", c(org.sagebionetworks.repo.model.dataaccess.AccessRequirementSearchSort$new("field_example", "direction_example")), "nextPageToken_example") # OrgSagebionetworksRepoModelDataaccessAccessRequirementSearchRequest | 
#'
#' api_instance <- AccessRequirementServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1AccessRequirementSearch(var_org_sagebionetworks_repo_model_dataaccess_access_requirement_search_requestdata_file = "result.txt")
#' result <- api_instance$postRepoV1AccessRequirementSearch(var_org_sagebionetworks_repo_model_dataaccess_access_requirement_search_request)
#' dput(result)
#'
#'
#' ####################  postRepoV1EntityIdLockAccessRequirement  ####################
#'
#' library(synr)
#' var_id <- "id_example" # character | the ID of the entity to which an Access Requirement will be applied
#'
#' api_instance <- AccessRequirementServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1EntityIdLockAccessRequirement(var_iddata_file = "result.txt")
#' result <- api_instance$postRepoV1EntityIdLockAccessRequirement(var_id)
#' dput(result)
#'
#'
#' ####################  putRepoV1AccessRequirementConversion  ####################
#'
#' library(synr)
#' var_org_sagebionetworks_repo_model_dataaccess_access_requirement_conversion_request <- org.sagebionetworks.repo.model.dataaccess.AccessRequirementConversionRequest$new("accessRequirementId_example", "etag_example", 123) # OrgSagebionetworksRepoModelDataaccessAccessRequirementConversionRequest | 
#'
#' api_instance <- AccessRequirementServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$putRepoV1AccessRequirementConversion(var_org_sagebionetworks_repo_model_dataaccess_access_requirement_conversion_requestdata_file = "result.txt")
#' result <- api_instance$putRepoV1AccessRequirementConversion(var_org_sagebionetworks_repo_model_dataaccess_access_requirement_conversion_request)
#' dput(result)
#'
#'
#' ####################  putRepoV1AccessRequirementRequirementId  ####################
#'
#' library(synr)
#' var_requirement_id <- "requirement_id_example" # character | the ID of the Access Requirement to be modified.
#' var_org_sagebionetworks_repo_model_access_requirement <- org.sagebionetworks.repo.model.AccessRequirement$new("concreteType_example", 123, 123, "description_example", "name_example", "etag_example", "createdOn_example", "modifiedOn_example", "createdBy_example", "modifiedBy_example", "subjectsDefinedByAnnotations_example", c(org.sagebionetworks.repo.model.RestrictableObjectDescriptor$new("id_example", "type_example")), "accessType_example", "url_example", "termsOfUse_example", "isCertifiedUserRequired_example", "isValidatedProfileRequired_example", "isDUCRequired_example", "ducTemplateFileHandleId_example", "isIRBApprovalRequired_example", "areOtherAttachmentsRequired_example", 123, "isIDUPublic_example", "isIDURequired_example", "isTwoFaRequired_example", "actContactInfo_example", "openJiraIssue_example", "jiraKey_example") # OrgSagebionetworksRepoModelAccessRequirement | 
#'
#' api_instance <- AccessRequirementServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$putRepoV1AccessRequirementRequirementId(var_requirement_id, var_org_sagebionetworks_repo_model_access_requirementdata_file = "result.txt")
#' result <- api_instance$putRepoV1AccessRequirementRequirementId(var_requirement_id, var_org_sagebionetworks_repo_model_access_requirement)
#' dput(result)
#'
#'
#' ####################  putRepoV1AccessRequirementRequirementIdAcl  ####################
#'
#' library(synr)
#' var_requirement_id <- "requirement_id_example" # character | 
#' var_org_sagebionetworks_repo_model_access_control_list <- org.sagebionetworks.repo.model.AccessControlList$new("id_example", "createdBy_example", "creationDate_example", "modifiedBy_example", "modifiedOn_example", "etag_example", c(org.sagebionetworks.repo.model.ResourceAccess$new(123, c("accessType_example")))) # OrgSagebionetworksRepoModelAccessControlList | 
#'
#' api_instance <- AccessRequirementServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$putRepoV1AccessRequirementRequirementIdAcl(var_requirement_id, var_org_sagebionetworks_repo_model_access_control_listdata_file = "result.txt")
#' result <- api_instance$putRepoV1AccessRequirementRequirementIdAcl(var_requirement_id, var_org_sagebionetworks_repo_model_access_control_list)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AccessRequirementServicesApi <- R6::R6Class(
  "AccessRequirementServicesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new AccessRequirementServicesApi.
    #'
    #' @description
    #' Initialize a new AccessRequirementServicesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id the ID of the requirement to delete
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    deleteRepoV1AccessRequirementRequirementId = function(requirement_id, ...) {
      local_var_response <- self$deleteRepoV1AccessRequirementRequirementIdWithHttpInfo(requirement_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id the ID of the requirement to delete
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    deleteRepoV1AccessRequirementRequirementIdWithHttpInfo = function(requirement_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`requirement_id`)) {
        stop("Missing required parameter `requirement_id`.")
      }


      local_var_url_path <- "/repo/v1/accessRequirement/{requirementId}"
      if (!missing(`requirement_id`)) {
        local_var_url_path <- gsub("\\{requirementId\\}", URLencode(as.character(`requirement_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    deleteRepoV1AccessRequirementRequirementIdAcl = function(requirement_id, ...) {
      local_var_response <- self$deleteRepoV1AccessRequirementRequirementIdAclWithHttpInfo(requirement_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    deleteRepoV1AccessRequirementRequirementIdAclWithHttpInfo = function(requirement_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`requirement_id`)) {
        stop("Missing required parameter `requirement_id`.")
      }


      local_var_url_path <- "/repo/v1/accessRequirement/{requirementId}/acl"
      if (!missing(`requirement_id`)) {
        local_var_url_path <- gsub("\\{requirementId\\}", URLencode(as.character(`requirement_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelAccessRequirement
    #' @export
    getRepoV1AccessRequirementRequirementId = function(requirement_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1AccessRequirementRequirementIdWithHttpInfo(requirement_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelAccessRequirement) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1AccessRequirementRequirementIdWithHttpInfo = function(requirement_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`requirement_id`)) {
        stop("Missing required parameter `requirement_id`.")
      }


      local_var_url_path <- "/repo/v1/accessRequirement/{requirementId}"
      if (!missing(`requirement_id`)) {
        local_var_url_path <- gsub("\\{requirementId\\}", URLencode(as.character(`requirement_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelAccessRequirement", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelAccessControlList
    #' @export
    getRepoV1AccessRequirementRequirementIdAcl = function(requirement_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1AccessRequirementRequirementIdAclWithHttpInfo(requirement_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelAccessControlList) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1AccessRequirementRequirementIdAclWithHttpInfo = function(requirement_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`requirement_id`)) {
        stop("Missing required parameter `requirement_id`.")
      }


      local_var_url_path <- "/repo/v1/accessRequirement/{requirementId}/acl"
      if (!missing(`requirement_id`)) {
        local_var_url_path <- gsub("\\{requirementId\\}", URLencode(as.character(`requirement_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelAccessControlList", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id 
    #' @param next_page_token (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelRestrictableObjectDescriptorResponse
    #' @export
    getRepoV1AccessRequirementRequirementIdSubjects = function(requirement_id, next_page_token = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1AccessRequirementRequirementIdSubjectsWithHttpInfo(requirement_id, next_page_token, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id 
    #' @param next_page_token (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelRestrictableObjectDescriptorResponse) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1AccessRequirementRequirementIdSubjectsWithHttpInfo = function(requirement_id, next_page_token = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`requirement_id`)) {
        stop("Missing required parameter `requirement_id`.")
      }



      query_params[["nextPageToken"]] <- `next_page_token`

      local_var_url_path <- "/repo/v1/accessRequirement/{requirementId}/subjects"
      if (!missing(`requirement_id`)) {
        local_var_url_path <- gsub("\\{requirementId\\}", URLencode(as.character(`requirement_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelRestrictableObjectDescriptorResponse", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param limit (optional) - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum limit for this call is 50.
    #' @param offset (optional) - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfAccessRequirement
    #' @export
    getRepoV1EntityIdAccessRequirement = function(id, limit = NULL, offset = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EntityIdAccessRequirementWithHttpInfo(id, limit, offset, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param limit (optional) - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum limit for this call is 50.
    #' @param offset (optional) - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfAccessRequirement) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EntityIdAccessRequirementWithHttpInfo = function(id, limit = NULL, offset = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }




      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      local_var_url_path <- "/repo/v1/entity/{id}/accessRequirement"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfAccessRequirement", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id the ID of the Team whose Access Requirements are retrieved.
    #' @param limit (optional) - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum limit for this call is 50.
    #' @param offset (optional) - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfAccessRequirement
    #' @export
    getRepoV1TeamIdAccessRequirement = function(id, limit = NULL, offset = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1TeamIdAccessRequirementWithHttpInfo(id, limit, offset, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id the ID of the Team whose Access Requirements are retrieved.
    #' @param limit (optional) - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum limit for this call is 50.
    #' @param offset (optional) - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfAccessRequirement) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1TeamIdAccessRequirementWithHttpInfo = function(id, limit = NULL, offset = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }




      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      local_var_url_path <- "/repo/v1/team/{id}/accessRequirement"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfAccessRequirement", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_access_requirement 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelAccessRequirement
    #' @export
    postRepoV1AccessRequirement = function(org_sagebionetworks_repo_model_access_requirement, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1AccessRequirementWithHttpInfo(org_sagebionetworks_repo_model_access_requirement, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_access_requirement 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelAccessRequirement) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1AccessRequirementWithHttpInfo = function(org_sagebionetworks_repo_model_access_requirement, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_access_requirement`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_access_requirement`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_access_requirement`)) {
        local_var_body <- `org_sagebionetworks_repo_model_access_requirement`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/accessRequirement"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelAccessRequirement", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id 
    #' @param org_sagebionetworks_repo_model_access_control_list 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelAccessControlList
    #' @export
    postRepoV1AccessRequirementRequirementIdAcl = function(requirement_id, org_sagebionetworks_repo_model_access_control_list, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1AccessRequirementRequirementIdAclWithHttpInfo(requirement_id, org_sagebionetworks_repo_model_access_control_list, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id 
    #' @param org_sagebionetworks_repo_model_access_control_list 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelAccessControlList) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1AccessRequirementRequirementIdAclWithHttpInfo = function(requirement_id, org_sagebionetworks_repo_model_access_control_list, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`requirement_id`)) {
        stop("Missing required parameter `requirement_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_access_control_list`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_access_control_list`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_access_control_list`)) {
        local_var_body <- `org_sagebionetworks_repo_model_access_control_list`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/accessRequirement/{requirementId}/acl"
      if (!missing(`requirement_id`)) {
        local_var_url_path <- gsub("\\{requirementId\\}", URLencode(as.character(`requirement_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelAccessControlList", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_dataaccess_access_requirement_search_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDataaccessAccessRequirementSearchResponse
    #' @export
    postRepoV1AccessRequirementSearch = function(org_sagebionetworks_repo_model_dataaccess_access_requirement_search_request, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1AccessRequirementSearchWithHttpInfo(org_sagebionetworks_repo_model_dataaccess_access_requirement_search_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_dataaccess_access_requirement_search_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDataaccessAccessRequirementSearchResponse) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1AccessRequirementSearchWithHttpInfo = function(org_sagebionetworks_repo_model_dataaccess_access_requirement_search_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_dataaccess_access_requirement_search_request`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_dataaccess_access_requirement_search_request`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_dataaccess_access_requirement_search_request`)) {
        local_var_body <- `org_sagebionetworks_repo_model_dataaccess_access_requirement_search_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/accessRequirement/search"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDataaccessAccessRequirementSearchResponse", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id the ID of the entity to which an Access Requirement will be applied
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelAccessRequirement
    #' @export
    postRepoV1EntityIdLockAccessRequirement = function(id, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1EntityIdLockAccessRequirementWithHttpInfo(id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id the ID of the entity to which an Access Requirement will be applied
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelAccessRequirement) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1EntityIdLockAccessRequirementWithHttpInfo = function(id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }


      local_var_url_path <- "/repo/v1/entity/{id}/lockAccessRequirement"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelAccessRequirement", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_dataaccess_access_requirement_conversion_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelAccessRequirement
    #' @export
    putRepoV1AccessRequirementConversion = function(org_sagebionetworks_repo_model_dataaccess_access_requirement_conversion_request, data_file = NULL, ...) {
      local_var_response <- self$putRepoV1AccessRequirementConversionWithHttpInfo(org_sagebionetworks_repo_model_dataaccess_access_requirement_conversion_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_dataaccess_access_requirement_conversion_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelAccessRequirement) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1AccessRequirementConversionWithHttpInfo = function(org_sagebionetworks_repo_model_dataaccess_access_requirement_conversion_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_dataaccess_access_requirement_conversion_request`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_dataaccess_access_requirement_conversion_request`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_dataaccess_access_requirement_conversion_request`)) {
        local_var_body <- `org_sagebionetworks_repo_model_dataaccess_access_requirement_conversion_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/accessRequirement/conversion"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelAccessRequirement", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id the ID of the Access Requirement to be modified.
    #' @param org_sagebionetworks_repo_model_access_requirement 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelAccessRequirement
    #' @export
    putRepoV1AccessRequirementRequirementId = function(requirement_id, org_sagebionetworks_repo_model_access_requirement, data_file = NULL, ...) {
      local_var_response <- self$putRepoV1AccessRequirementRequirementIdWithHttpInfo(requirement_id, org_sagebionetworks_repo_model_access_requirement, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id the ID of the Access Requirement to be modified.
    #' @param org_sagebionetworks_repo_model_access_requirement 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelAccessRequirement) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1AccessRequirementRequirementIdWithHttpInfo = function(requirement_id, org_sagebionetworks_repo_model_access_requirement, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`requirement_id`)) {
        stop("Missing required parameter `requirement_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_access_requirement`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_access_requirement`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_access_requirement`)) {
        local_var_body <- `org_sagebionetworks_repo_model_access_requirement`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/accessRequirement/{requirementId}"
      if (!missing(`requirement_id`)) {
        local_var_url_path <- gsub("\\{requirementId\\}", URLencode(as.character(`requirement_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelAccessRequirement", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id 
    #' @param org_sagebionetworks_repo_model_access_control_list 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelAccessControlList
    #' @export
    putRepoV1AccessRequirementRequirementIdAcl = function(requirement_id, org_sagebionetworks_repo_model_access_control_list, data_file = NULL, ...) {
      local_var_response <- self$putRepoV1AccessRequirementRequirementIdAclWithHttpInfo(requirement_id, org_sagebionetworks_repo_model_access_control_list, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param requirement_id 
    #' @param org_sagebionetworks_repo_model_access_control_list 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelAccessControlList) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1AccessRequirementRequirementIdAclWithHttpInfo = function(requirement_id, org_sagebionetworks_repo_model_access_control_list, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`requirement_id`)) {
        stop("Missing required parameter `requirement_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_access_control_list`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_access_control_list`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_access_control_list`)) {
        local_var_body <- `org_sagebionetworks_repo_model_access_control_list`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/accessRequirement/{requirementId}/acl"
      if (!missing(`requirement_id`)) {
        local_var_url_path <- gsub("\\{requirementId\\}", URLencode(as.character(`requirement_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelAccessControlList", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
