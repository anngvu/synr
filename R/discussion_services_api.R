#' Synapse REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title DiscussionServices operations
#' @description DiscussionServicesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ deleteRepoV1ReplyReplyId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } reply_id character
#'
#'
#' \item status code : 204 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ deleteRepoV1ThreadThreadId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#'
#'
#' \item status code : 204 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EntityIdThreads } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } limit numeric
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } id character
#' \item \emph{ @param } sort \link{OrgSagebionetworksRepoModelDiscussionDiscussionThreadOrder}
#' \item \emph{ @param } ascending character
#' \item \emph{ @returnType } \link{PaginatedResultsOfDiscussionThreadBundle} \cr
#'
#'
#' \item status code : 200 | the threads that user has read permission to.
#'
#' \item return type : PaginatedResultsOfDiscussionThreadBundle
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1ForumForumId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } forum_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDiscussionForum} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDiscussionForum
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1ForumForumIdModerators } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } limit numeric
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } forum_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelPaginatedIds} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelPaginatedIds
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1ForumForumIdThreadcount } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } filter \link{OrgSagebionetworksRepoModelDiscussionDiscussionFilter}
#' \item \emph{ @param } forum_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDiscussionThreadCount} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDiscussionThreadCount
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1ForumForumIdThreads } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } limit numeric
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } filter \link{OrgSagebionetworksRepoModelDiscussionDiscussionFilter}
#' \item \emph{ @param } forum_id character
#' \item \emph{ @param } sort \link{OrgSagebionetworksRepoModelDiscussionDiscussionThreadOrder}
#' \item \emph{ @param } ascending character
#' \item \emph{ @returnType } \link{PaginatedResultsOfDiscussionThreadBundle} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfDiscussionThreadBundle
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1ProjectProjectIdForum } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDiscussionForum} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDiscussionForum
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1ReplyMessageUrl } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } message_key character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDiscussionMessageURL} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDiscussionMessageURL
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1ReplyReplyId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } reply_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDiscussionDiscussionReplyBundle} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDiscussionDiscussionReplyBundle
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1ThreadMessageUrl } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } message_key character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDiscussionMessageURL} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDiscussionMessageURL
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1ThreadThreadId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1ThreadThreadIdReplies } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } limit numeric
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } filter \link{OrgSagebionetworksRepoModelDiscussionDiscussionFilter}
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } sort \link{OrgSagebionetworksRepoModelDiscussionDiscussionReplyOrder}
#' \item \emph{ @param } ascending character
#' \item \emph{ @returnType } \link{PaginatedResultsOfDiscussionReplyBundle} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfDiscussionReplyBundle
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1ThreadThreadIdReplycount } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } filter \link{OrgSagebionetworksRepoModelDiscussionDiscussionFilter}
#' \item \emph{ @param } thread_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDiscussionReplyCount} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDiscussionReplyCount
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1EntityThreadcounts } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_entity_id_list \link{OrgSagebionetworksRepoModelEntityIdList}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDiscussionEntityThreadCounts} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDiscussionEntityThreadCounts
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1ForumForumIdSearch } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } forum_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_discussion_discussion_search_request \link{OrgSagebionetworksRepoModelDiscussionDiscussionSearchRequest}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDiscussionDiscussionSearchResponse} \cr
#'
#'
#' \item status code : 200 | The response containing a page of results ordered by relevance and a potential token to fetch the results for a subsequent page
#'
#' \item return type : OrgSagebionetworksRepoModelDiscussionDiscussionSearchResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1Reply } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_discussion_create_discussion_reply \link{OrgSagebionetworksRepoModelDiscussionCreateDiscussionReply}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDiscussionDiscussionReplyBundle} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDiscussionDiscussionReplyBundle
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1Thread } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_discussion_create_discussion_thread \link{OrgSagebionetworksRepoModelDiscussionCreateDiscussionThread}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1ReplyReplyIdMessage } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } reply_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_discussion_update_reply_message \link{OrgSagebionetworksRepoModelDiscussionUpdateReplyMessage}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDiscussionDiscussionReplyBundle} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDiscussionDiscussionReplyBundle
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1ThreadThreadIdMessage } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_discussion_update_thread_message \link{OrgSagebionetworksRepoModelDiscussionUpdateThreadMessage}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1ThreadThreadIdPin } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#'
#'
#' \item status code : 204 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1ThreadThreadIdRestore } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#'
#'
#' \item status code : 200 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1ThreadThreadIdTitle } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_discussion_update_thread_title \link{OrgSagebionetworksRepoModelDiscussionUpdateThreadTitle}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1ThreadThreadIdUnpin } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } thread_id character
#'
#'
#' \item status code : 204 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  deleteRepoV1ReplyReplyId  ####################
#'
#' library(synr)
#' var_reply_id <- "reply_id_example" # character | - the ID of the reply being marked as deleted
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$deleteRepoV1ReplyReplyId(var_reply_id)
#'
#'
#' ####################  deleteRepoV1ThreadThreadId  ####################
#'
#' library(synr)
#' var_thread_id <- "thread_id_example" # character | - the ID of the thread being marked as deleted
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$deleteRepoV1ThreadThreadId(var_thread_id)
#'
#'
#' ####################  getRepoV1EntityIdThreads  ####################
#'
#' library(synr)
#' var_limit <- 3.4 # numeric | - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum Limit for this call is 20.
#' var_offset <- 3.4 # numeric | - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
#' var_id <- "id_example" # character | - The request entityId
#' var_sort <- org.sagebionetworks.repo.model.discussion.DiscussionThreadOrder$new() # OrgSagebionetworksRepoModelDiscussionDiscussionThreadOrder | - The field to sort the resulting threads on. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionThreadOrder}\">,DiscussionThreadOrder,</a>,. (Optional)
#' var_ascending <- "ascending_example" # character | - The direction of sort: true for ascending, and false for descending (Optional)
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EntityIdThreads(var_limit, var_offset, var_id, sort = var_sort, ascending = var_ascendingdata_file = "result.txt")
#' result <- api_instance$getRepoV1EntityIdThreads(var_limit, var_offset, var_id, sort = var_sort, ascending = var_ascending)
#' dput(result)
#'
#'
#' ####################  getRepoV1ForumForumId  ####################
#'
#' library(synr)
#' var_forum_id <- "forum_id_example" # character | - The ID of the forum.
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1ForumForumId(var_forum_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1ForumForumId(var_forum_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1ForumForumIdModerators  ####################
#'
#' library(synr)
#' var_limit <- 3.4 # numeric | - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum Limit for this call is 100.
#' var_offset <- 3.4 # numeric | - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
#' var_forum_id <- "forum_id_example" # character | - The forum ID to which the returning mederators belong
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1ForumForumIdModerators(var_limit, var_offset, var_forum_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1ForumForumIdModerators(var_limit, var_offset, var_forum_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1ForumForumIdThreadcount  ####################
#'
#' library(synr)
#' var_filter <- org.sagebionetworks.repo.model.discussion.DiscussionFilter$new() # OrgSagebionetworksRepoModelDiscussionDiscussionFilter | - Filter deleted/ not deleted threads. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionFilter}\">,DiscussionFilter,</a>,.
#' var_forum_id <- "forum_id_example" # character | - The forum ID to which the returning threads belong
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1ForumForumIdThreadcount(var_filter, var_forum_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1ForumForumIdThreadcount(var_filter, var_forum_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1ForumForumIdThreads  ####################
#'
#' library(synr)
#' var_limit <- 3.4 # numeric | - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum Limit for this call is 20.
#' var_offset <- 3.4 # numeric | - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
#' var_filter <- org.sagebionetworks.repo.model.discussion.DiscussionFilter$new() # OrgSagebionetworksRepoModelDiscussionDiscussionFilter | - Filter deleted/ not deleted threads. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionFilter}\">,DiscussionFilter,</a>,.
#' var_forum_id <- "forum_id_example" # character | - The forum ID to which the returning threads belong
#' var_sort <- org.sagebionetworks.repo.model.discussion.DiscussionThreadOrder$new() # OrgSagebionetworksRepoModelDiscussionDiscussionThreadOrder | - The field to sort the resulting threads on. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionThreadOrder}\">,DiscussionThreadOrder,</a>,. (Optional)
#' var_ascending <- "ascending_example" # character | - The direction of sort: true for ascending, and false for descending (Optional)
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1ForumForumIdThreads(var_limit, var_offset, var_filter, var_forum_id, sort = var_sort, ascending = var_ascendingdata_file = "result.txt")
#' result <- api_instance$getRepoV1ForumForumIdThreads(var_limit, var_offset, var_filter, var_forum_id, sort = var_sort, ascending = var_ascending)
#' dput(result)
#'
#'
#' ####################  getRepoV1ProjectProjectIdForum  ####################
#'
#' library(synr)
#' var_project_id <- "project_id_example" # character | - The ID of the project to which the forum belongs.
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1ProjectProjectIdForum(var_project_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1ProjectProjectIdForum(var_project_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1ReplyMessageUrl  ####################
#'
#' library(synr)
#' var_message_key <- "message_key_example" # character | - DiscussionReplyBundle.messageKey
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1ReplyMessageUrl(var_message_keydata_file = "result.txt")
#' result <- api_instance$getRepoV1ReplyMessageUrl(var_message_key)
#' dput(result)
#'
#'
#' ####################  getRepoV1ReplyReplyId  ####################
#'
#' library(synr)
#' var_reply_id <- "reply_id_example" # character | - The ID of the reply being requested
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1ReplyReplyId(var_reply_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1ReplyReplyId(var_reply_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1ThreadMessageUrl  ####################
#'
#' library(synr)
#' var_message_key <- "message_key_example" # character | 
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1ThreadMessageUrl(var_message_keydata_file = "result.txt")
#' result <- api_instance$getRepoV1ThreadMessageUrl(var_message_key)
#' dput(result)
#'
#'
#' ####################  getRepoV1ThreadThreadId  ####################
#'
#' library(synr)
#' var_thread_id <- "thread_id_example" # character | - The ID of the thread being requested
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1ThreadThreadId(var_thread_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1ThreadThreadId(var_thread_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1ThreadThreadIdReplies  ####################
#'
#' library(synr)
#' var_limit <- 3.4 # numeric | - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum Limit for this call is 100.
#' var_offset <- 3.4 # numeric | - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
#' var_filter <- org.sagebionetworks.repo.model.discussion.DiscussionFilter$new() # OrgSagebionetworksRepoModelDiscussionDiscussionFilter | - Filter deleted/ not deleted replies. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionFilter}\">,DiscussionFilter,</a>,.
#' var_thread_id <- "thread_id_example" # character | - The thread ID to which the returning replies belong
#' var_sort <- org.sagebionetworks.repo.model.discussion.DiscussionReplyOrder$new() # OrgSagebionetworksRepoModelDiscussionDiscussionReplyOrder | - The field to sort the resulting replies on. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionReplyOrder}\">,DiscussionReplyOrder,</a>,. (Optional)
#' var_ascending <- "ascending_example" # character | - The direction of sort: true for ascending, and false for descending (Optional)
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1ThreadThreadIdReplies(var_limit, var_offset, var_filter, var_thread_id, sort = var_sort, ascending = var_ascendingdata_file = "result.txt")
#' result <- api_instance$getRepoV1ThreadThreadIdReplies(var_limit, var_offset, var_filter, var_thread_id, sort = var_sort, ascending = var_ascending)
#' dput(result)
#'
#'
#' ####################  getRepoV1ThreadThreadIdReplycount  ####################
#'
#' library(synr)
#' var_filter <- org.sagebionetworks.repo.model.discussion.DiscussionFilter$new() # OrgSagebionetworksRepoModelDiscussionDiscussionFilter | - Filter deleted/ not deleted replies. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionFilter}\">,DiscussionFilter,</a>,.
#' var_thread_id <- "thread_id_example" # character | - The thread ID to which the returning replies belong
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1ThreadThreadIdReplycount(var_filter, var_thread_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1ThreadThreadIdReplycount(var_filter, var_thread_id)
#' dput(result)
#'
#'
#' ####################  postRepoV1EntityThreadcounts  ####################
#'
#' library(synr)
#' var_org_sagebionetworks_repo_model_entity_id_list <- org.sagebionetworks.repo.model.EntityIdList$new(c("idList_example")) # OrgSagebionetworksRepoModelEntityIdList | 
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1EntityThreadcounts(var_org_sagebionetworks_repo_model_entity_id_listdata_file = "result.txt")
#' result <- api_instance$postRepoV1EntityThreadcounts(var_org_sagebionetworks_repo_model_entity_id_list)
#' dput(result)
#'
#'
#' ####################  postRepoV1ForumForumIdSearch  ####################
#'
#' library(synr)
#' var_forum_id <- "forum_id_example" # character | The ID of the forum where the search is performed
#' var_org_sagebionetworks_repo_model_discussion_discussion_search_request <- org.sagebionetworks.repo.model.discussion.DiscussionSearchRequest$new("searchString_example", "nextPageToken_example") # OrgSagebionetworksRepoModelDiscussionDiscussionSearchRequest | 
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1ForumForumIdSearch(var_forum_id, var_org_sagebionetworks_repo_model_discussion_discussion_search_requestdata_file = "result.txt")
#' result <- api_instance$postRepoV1ForumForumIdSearch(var_forum_id, var_org_sagebionetworks_repo_model_discussion_discussion_search_request)
#' dput(result)
#'
#'
#' ####################  postRepoV1Reply  ####################
#'
#' library(synr)
#' var_org_sagebionetworks_repo_model_discussion_create_discussion_reply <- org.sagebionetworks.repo.model.discussion.CreateDiscussionReply$new("threadId_example", "messageMarkdown_example") # OrgSagebionetworksRepoModelDiscussionCreateDiscussionReply | 
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1Reply(var_org_sagebionetworks_repo_model_discussion_create_discussion_replydata_file = "result.txt")
#' result <- api_instance$postRepoV1Reply(var_org_sagebionetworks_repo_model_discussion_create_discussion_reply)
#' dput(result)
#'
#'
#' ####################  postRepoV1Thread  ####################
#'
#' library(synr)
#' var_org_sagebionetworks_repo_model_discussion_create_discussion_thread <- org.sagebionetworks.repo.model.discussion.CreateDiscussionThread$new("forumId_example", "title_example", "messageMarkdown_example") # OrgSagebionetworksRepoModelDiscussionCreateDiscussionThread | 
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1Thread(var_org_sagebionetworks_repo_model_discussion_create_discussion_threaddata_file = "result.txt")
#' result <- api_instance$postRepoV1Thread(var_org_sagebionetworks_repo_model_discussion_create_discussion_thread)
#' dput(result)
#'
#'
#' ####################  putRepoV1ReplyReplyIdMessage  ####################
#'
#' library(synr)
#' var_reply_id <- "reply_id_example" # character | - The ID of the reply being updated
#' var_org_sagebionetworks_repo_model_discussion_update_reply_message <- org.sagebionetworks.repo.model.discussion.UpdateReplyMessage$new("messageMarkdown_example") # OrgSagebionetworksRepoModelDiscussionUpdateReplyMessage | 
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$putRepoV1ReplyReplyIdMessage(var_reply_id, var_org_sagebionetworks_repo_model_discussion_update_reply_messagedata_file = "result.txt")
#' result <- api_instance$putRepoV1ReplyReplyIdMessage(var_reply_id, var_org_sagebionetworks_repo_model_discussion_update_reply_message)
#' dput(result)
#'
#'
#' ####################  putRepoV1ThreadThreadIdMessage  ####################
#'
#' library(synr)
#' var_thread_id <- "thread_id_example" # character | - The ID of the thread being updated
#' var_org_sagebionetworks_repo_model_discussion_update_thread_message <- org.sagebionetworks.repo.model.discussion.UpdateThreadMessage$new("messageMarkdown_example") # OrgSagebionetworksRepoModelDiscussionUpdateThreadMessage | 
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$putRepoV1ThreadThreadIdMessage(var_thread_id, var_org_sagebionetworks_repo_model_discussion_update_thread_messagedata_file = "result.txt")
#' result <- api_instance$putRepoV1ThreadThreadIdMessage(var_thread_id, var_org_sagebionetworks_repo_model_discussion_update_thread_message)
#' dput(result)
#'
#'
#' ####################  putRepoV1ThreadThreadIdPin  ####################
#'
#' library(synr)
#' var_thread_id <- "thread_id_example" # character | - the ID of the thread being marked as pinned
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$putRepoV1ThreadThreadIdPin(var_thread_id)
#'
#'
#' ####################  putRepoV1ThreadThreadIdRestore  ####################
#'
#' library(synr)
#' var_thread_id <- "thread_id_example" # character | - the ID of the thread that was marked as deleted
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$putRepoV1ThreadThreadIdRestore(var_thread_id)
#'
#'
#' ####################  putRepoV1ThreadThreadIdTitle  ####################
#'
#' library(synr)
#' var_thread_id <- "thread_id_example" # character | - The ID of the thread being updated
#' var_org_sagebionetworks_repo_model_discussion_update_thread_title <- org.sagebionetworks.repo.model.discussion.UpdateThreadTitle$new("title_example") # OrgSagebionetworksRepoModelDiscussionUpdateThreadTitle | 
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$putRepoV1ThreadThreadIdTitle(var_thread_id, var_org_sagebionetworks_repo_model_discussion_update_thread_titledata_file = "result.txt")
#' result <- api_instance$putRepoV1ThreadThreadIdTitle(var_thread_id, var_org_sagebionetworks_repo_model_discussion_update_thread_title)
#' dput(result)
#'
#'
#' ####################  putRepoV1ThreadThreadIdUnpin  ####################
#'
#' library(synr)
#' var_thread_id <- "thread_id_example" # character | - the ID of the thread being unpinned
#'
#' api_instance <- DiscussionServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$putRepoV1ThreadThreadIdUnpin(var_thread_id)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
DiscussionServicesApi <- R6::R6Class(
  "DiscussionServicesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new DiscussionServicesApi.
    #'
    #' @description
    #' Initialize a new DiscussionServicesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param reply_id - the ID of the reply being marked as deleted
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    deleteRepoV1ReplyReplyId = function(reply_id, ...) {
      local_var_response <- self$deleteRepoV1ReplyReplyIdWithHttpInfo(reply_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param reply_id - the ID of the reply being marked as deleted
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    deleteRepoV1ReplyReplyIdWithHttpInfo = function(reply_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`reply_id`)) {
        stop("Missing required parameter `reply_id`.")
      }


      local_var_url_path <- "/repo/v1/reply/{replyId}"
      if (!missing(`reply_id`)) {
        local_var_url_path <- gsub("\\{replyId\\}", URLencode(as.character(`reply_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param thread_id - the ID of the thread being marked as deleted
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    deleteRepoV1ThreadThreadId = function(thread_id, ...) {
      local_var_response <- self$deleteRepoV1ThreadThreadIdWithHttpInfo(thread_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param thread_id - the ID of the thread being marked as deleted
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    deleteRepoV1ThreadThreadIdWithHttpInfo = function(thread_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }


      local_var_url_path <- "/repo/v1/thread/{threadId}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{threadId\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param limit - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum Limit for this call is 20.
    #' @param offset - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
    #' @param id - The request entityId
    #' @param sort (optional) - The field to sort the resulting threads on. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionThreadOrder}\">,DiscussionThreadOrder,</a>,.
    #' @param ascending (optional) - The direction of sort: true for ascending, and false for descending
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfDiscussionThreadBundle
    #' @export
    getRepoV1EntityIdThreads = function(limit, offset, id, sort = NULL, ascending = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EntityIdThreadsWithHttpInfo(limit, offset, id, sort, ascending, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param limit - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum Limit for this call is 20.
    #' @param offset - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
    #' @param id - The request entityId
    #' @param sort (optional) - The field to sort the resulting threads on. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionThreadOrder}\">,DiscussionThreadOrder,</a>,.
    #' @param ascending (optional) - The direction of sort: true for ascending, and false for descending
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfDiscussionThreadBundle) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EntityIdThreadsWithHttpInfo = function(limit, offset, id, sort = NULL, ascending = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`offset`)) {
        stop("Missing required parameter `offset`.")
      }

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }






      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      query_params[["sort"]] <- `sort`

      query_params[["ascending"]] <- `ascending`

      local_var_url_path <- "/repo/v1/entity/{id}/threads"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfDiscussionThreadBundle", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param forum_id - The ID of the forum.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDiscussionForum
    #' @export
    getRepoV1ForumForumId = function(forum_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1ForumForumIdWithHttpInfo(forum_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param forum_id - The ID of the forum.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDiscussionForum) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1ForumForumIdWithHttpInfo = function(forum_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`forum_id`)) {
        stop("Missing required parameter `forum_id`.")
      }


      local_var_url_path <- "/repo/v1/forum/{forumId}"
      if (!missing(`forum_id`)) {
        local_var_url_path <- gsub("\\{forumId\\}", URLencode(as.character(`forum_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDiscussionForum", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param limit - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum Limit for this call is 100.
    #' @param offset - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
    #' @param forum_id - The forum ID to which the returning mederators belong
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelPaginatedIds
    #' @export
    getRepoV1ForumForumIdModerators = function(limit, offset, forum_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1ForumForumIdModeratorsWithHttpInfo(limit, offset, forum_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param limit - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum Limit for this call is 100.
    #' @param offset - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
    #' @param forum_id - The forum ID to which the returning mederators belong
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelPaginatedIds) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1ForumForumIdModeratorsWithHttpInfo = function(limit, offset, forum_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`offset`)) {
        stop("Missing required parameter `offset`.")
      }

      if (missing(`forum_id`)) {
        stop("Missing required parameter `forum_id`.")
      }




      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      local_var_url_path <- "/repo/v1/forum/{forumId}/moderators"
      if (!missing(`forum_id`)) {
        local_var_url_path <- gsub("\\{forumId\\}", URLencode(as.character(`forum_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelPaginatedIds", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param filter - Filter deleted/ not deleted threads. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionFilter}\">,DiscussionFilter,</a>,.
    #' @param forum_id - The forum ID to which the returning threads belong
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDiscussionThreadCount
    #' @export
    getRepoV1ForumForumIdThreadcount = function(filter, forum_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1ForumForumIdThreadcountWithHttpInfo(filter, forum_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param filter - Filter deleted/ not deleted threads. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionFilter}\">,DiscussionFilter,</a>,.
    #' @param forum_id - The forum ID to which the returning threads belong
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDiscussionThreadCount) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1ForumForumIdThreadcountWithHttpInfo = function(filter, forum_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`filter`)) {
        stop("Missing required parameter `filter`.")
      }

      if (missing(`forum_id`)) {
        stop("Missing required parameter `forum_id`.")
      }



      query_params[["filter"]] <- `filter`

      local_var_url_path <- "/repo/v1/forum/{forumId}/threadcount"
      if (!missing(`forum_id`)) {
        local_var_url_path <- gsub("\\{forumId\\}", URLencode(as.character(`forum_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDiscussionThreadCount", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param limit - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum Limit for this call is 20.
    #' @param offset - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
    #' @param filter - Filter deleted/ not deleted threads. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionFilter}\">,DiscussionFilter,</a>,.
    #' @param forum_id - The forum ID to which the returning threads belong
    #' @param sort (optional) - The field to sort the resulting threads on. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionThreadOrder}\">,DiscussionThreadOrder,</a>,.
    #' @param ascending (optional) - The direction of sort: true for ascending, and false for descending
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfDiscussionThreadBundle
    #' @export
    getRepoV1ForumForumIdThreads = function(limit, offset, filter, forum_id, sort = NULL, ascending = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1ForumForumIdThreadsWithHttpInfo(limit, offset, filter, forum_id, sort, ascending, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param limit - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum Limit for this call is 20.
    #' @param offset - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
    #' @param filter - Filter deleted/ not deleted threads. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionFilter}\">,DiscussionFilter,</a>,.
    #' @param forum_id - The forum ID to which the returning threads belong
    #' @param sort (optional) - The field to sort the resulting threads on. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionThreadOrder}\">,DiscussionThreadOrder,</a>,.
    #' @param ascending (optional) - The direction of sort: true for ascending, and false for descending
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfDiscussionThreadBundle) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1ForumForumIdThreadsWithHttpInfo = function(limit, offset, filter, forum_id, sort = NULL, ascending = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`offset`)) {
        stop("Missing required parameter `offset`.")
      }

      if (missing(`filter`)) {
        stop("Missing required parameter `filter`.")
      }

      if (missing(`forum_id`)) {
        stop("Missing required parameter `forum_id`.")
      }







      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      query_params[["sort"]] <- `sort`

      query_params[["ascending"]] <- `ascending`

      query_params[["filter"]] <- `filter`

      local_var_url_path <- "/repo/v1/forum/{forumId}/threads"
      if (!missing(`forum_id`)) {
        local_var_url_path <- gsub("\\{forumId\\}", URLencode(as.character(`forum_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfDiscussionThreadBundle", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param project_id - The ID of the project to which the forum belongs.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDiscussionForum
    #' @export
    getRepoV1ProjectProjectIdForum = function(project_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1ProjectProjectIdForumWithHttpInfo(project_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param project_id - The ID of the project to which the forum belongs.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDiscussionForum) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1ProjectProjectIdForumWithHttpInfo = function(project_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }


      local_var_url_path <- "/repo/v1/project/{projectId}/forum"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDiscussionForum", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param message_key - DiscussionReplyBundle.messageKey
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDiscussionMessageURL
    #' @export
    getRepoV1ReplyMessageUrl = function(message_key, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1ReplyMessageUrlWithHttpInfo(message_key, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param message_key - DiscussionReplyBundle.messageKey
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDiscussionMessageURL) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1ReplyMessageUrlWithHttpInfo = function(message_key, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`message_key`)) {
        stop("Missing required parameter `message_key`.")
      }


      query_params[["messageKey"]] <- `message_key`

      local_var_url_path <- "/repo/v1/reply/messageUrl"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDiscussionMessageURL", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param reply_id - The ID of the reply being requested
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDiscussionDiscussionReplyBundle
    #' @export
    getRepoV1ReplyReplyId = function(reply_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1ReplyReplyIdWithHttpInfo(reply_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param reply_id - The ID of the reply being requested
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDiscussionDiscussionReplyBundle) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1ReplyReplyIdWithHttpInfo = function(reply_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`reply_id`)) {
        stop("Missing required parameter `reply_id`.")
      }


      local_var_url_path <- "/repo/v1/reply/{replyId}"
      if (!missing(`reply_id`)) {
        local_var_url_path <- gsub("\\{replyId\\}", URLencode(as.character(`reply_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDiscussionDiscussionReplyBundle", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param message_key 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDiscussionMessageURL
    #' @export
    getRepoV1ThreadMessageUrl = function(message_key, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1ThreadMessageUrlWithHttpInfo(message_key, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param message_key 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDiscussionMessageURL) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1ThreadMessageUrlWithHttpInfo = function(message_key, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`message_key`)) {
        stop("Missing required parameter `message_key`.")
      }


      query_params[["messageKey"]] <- `message_key`

      local_var_url_path <- "/repo/v1/thread/messageUrl"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDiscussionMessageURL", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param thread_id - The ID of the thread being requested
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle
    #' @export
    getRepoV1ThreadThreadId = function(thread_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1ThreadThreadIdWithHttpInfo(thread_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param thread_id - The ID of the thread being requested
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1ThreadThreadIdWithHttpInfo = function(thread_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }


      local_var_url_path <- "/repo/v1/thread/{threadId}"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{threadId\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param limit - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum Limit for this call is 100.
    #' @param offset - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
    #' @param filter - Filter deleted/ not deleted replies. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionFilter}\">,DiscussionFilter,</a>,.
    #' @param thread_id - The thread ID to which the returning replies belong
    #' @param sort (optional) - The field to sort the resulting replies on. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionReplyOrder}\">,DiscussionReplyOrder,</a>,.
    #' @param ascending (optional) - The direction of sort: true for ascending, and false for descending
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfDiscussionReplyBundle
    #' @export
    getRepoV1ThreadThreadIdReplies = function(limit, offset, filter, thread_id, sort = NULL, ascending = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1ThreadThreadIdRepliesWithHttpInfo(limit, offset, filter, thread_id, sort, ascending, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param limit - Limits the size of the page returned. For example, a page size of 10 require limit = 10. The maximum Limit for this call is 100.
    #' @param offset - The index of the pagination offset. For a page size of 10, the first page would be at offset = 0, and the second page would be at offset = 10.
    #' @param filter - Filter deleted/ not deleted replies. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionFilter}\">,DiscussionFilter,</a>,.
    #' @param thread_id - The thread ID to which the returning replies belong
    #' @param sort (optional) - The field to sort the resulting replies on. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionReplyOrder}\">,DiscussionReplyOrder,</a>,.
    #' @param ascending (optional) - The direction of sort: true for ascending, and false for descending
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfDiscussionReplyBundle) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1ThreadThreadIdRepliesWithHttpInfo = function(limit, offset, filter, thread_id, sort = NULL, ascending = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`offset`)) {
        stop("Missing required parameter `offset`.")
      }

      if (missing(`filter`)) {
        stop("Missing required parameter `filter`.")
      }

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }







      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      query_params[["sort"]] <- `sort`

      query_params[["ascending"]] <- `ascending`

      query_params[["filter"]] <- `filter`

      local_var_url_path <- "/repo/v1/thread/{threadId}/replies"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{threadId\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfDiscussionReplyBundle", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param filter - Filter deleted/ not deleted replies. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionFilter}\">,DiscussionFilter,</a>,.
    #' @param thread_id - The thread ID to which the returning replies belong
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDiscussionReplyCount
    #' @export
    getRepoV1ThreadThreadIdReplycount = function(filter, thread_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1ThreadThreadIdReplycountWithHttpInfo(filter, thread_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param filter - Filter deleted/ not deleted replies. Available options: ,<a href=\"${org.sagebionetworks.repo.model.discussion.DiscussionFilter}\">,DiscussionFilter,</a>,.
    #' @param thread_id - The thread ID to which the returning replies belong
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDiscussionReplyCount) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1ThreadThreadIdReplycountWithHttpInfo = function(filter, thread_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`filter`)) {
        stop("Missing required parameter `filter`.")
      }

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }



      query_params[["filter"]] <- `filter`

      local_var_url_path <- "/repo/v1/thread/{threadId}/replycount"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{threadId\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDiscussionReplyCount", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_entity_id_list 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDiscussionEntityThreadCounts
    #' @export
    postRepoV1EntityThreadcounts = function(org_sagebionetworks_repo_model_entity_id_list, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1EntityThreadcountsWithHttpInfo(org_sagebionetworks_repo_model_entity_id_list, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_entity_id_list 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDiscussionEntityThreadCounts) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1EntityThreadcountsWithHttpInfo = function(org_sagebionetworks_repo_model_entity_id_list, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_entity_id_list`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_entity_id_list`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_entity_id_list`)) {
        local_var_body <- `org_sagebionetworks_repo_model_entity_id_list`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/entity/threadcounts"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDiscussionEntityThreadCounts", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param forum_id The ID of the forum where the search is performed
    #' @param org_sagebionetworks_repo_model_discussion_discussion_search_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDiscussionDiscussionSearchResponse
    #' @export
    postRepoV1ForumForumIdSearch = function(forum_id, org_sagebionetworks_repo_model_discussion_discussion_search_request, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1ForumForumIdSearchWithHttpInfo(forum_id, org_sagebionetworks_repo_model_discussion_discussion_search_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param forum_id The ID of the forum where the search is performed
    #' @param org_sagebionetworks_repo_model_discussion_discussion_search_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDiscussionDiscussionSearchResponse) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1ForumForumIdSearchWithHttpInfo = function(forum_id, org_sagebionetworks_repo_model_discussion_discussion_search_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`forum_id`)) {
        stop("Missing required parameter `forum_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_discussion_discussion_search_request`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_discussion_discussion_search_request`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_discussion_discussion_search_request`)) {
        local_var_body <- `org_sagebionetworks_repo_model_discussion_discussion_search_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/forum/{forumId}/search"
      if (!missing(`forum_id`)) {
        local_var_url_path <- gsub("\\{forumId\\}", URLencode(as.character(`forum_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDiscussionDiscussionSearchResponse", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_discussion_create_discussion_reply 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDiscussionDiscussionReplyBundle
    #' @export
    postRepoV1Reply = function(org_sagebionetworks_repo_model_discussion_create_discussion_reply, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1ReplyWithHttpInfo(org_sagebionetworks_repo_model_discussion_create_discussion_reply, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_discussion_create_discussion_reply 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDiscussionDiscussionReplyBundle) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1ReplyWithHttpInfo = function(org_sagebionetworks_repo_model_discussion_create_discussion_reply, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_discussion_create_discussion_reply`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_discussion_create_discussion_reply`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_discussion_create_discussion_reply`)) {
        local_var_body <- `org_sagebionetworks_repo_model_discussion_create_discussion_reply`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/reply"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDiscussionDiscussionReplyBundle", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_discussion_create_discussion_thread 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle
    #' @export
    postRepoV1Thread = function(org_sagebionetworks_repo_model_discussion_create_discussion_thread, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1ThreadWithHttpInfo(org_sagebionetworks_repo_model_discussion_create_discussion_thread, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_discussion_create_discussion_thread 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1ThreadWithHttpInfo = function(org_sagebionetworks_repo_model_discussion_create_discussion_thread, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_discussion_create_discussion_thread`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_discussion_create_discussion_thread`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_discussion_create_discussion_thread`)) {
        local_var_body <- `org_sagebionetworks_repo_model_discussion_create_discussion_thread`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/thread"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param reply_id - The ID of the reply being updated
    #' @param org_sagebionetworks_repo_model_discussion_update_reply_message 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDiscussionDiscussionReplyBundle
    #' @export
    putRepoV1ReplyReplyIdMessage = function(reply_id, org_sagebionetworks_repo_model_discussion_update_reply_message, data_file = NULL, ...) {
      local_var_response <- self$putRepoV1ReplyReplyIdMessageWithHttpInfo(reply_id, org_sagebionetworks_repo_model_discussion_update_reply_message, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param reply_id - The ID of the reply being updated
    #' @param org_sagebionetworks_repo_model_discussion_update_reply_message 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDiscussionDiscussionReplyBundle) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1ReplyReplyIdMessageWithHttpInfo = function(reply_id, org_sagebionetworks_repo_model_discussion_update_reply_message, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`reply_id`)) {
        stop("Missing required parameter `reply_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_discussion_update_reply_message`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_discussion_update_reply_message`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_discussion_update_reply_message`)) {
        local_var_body <- `org_sagebionetworks_repo_model_discussion_update_reply_message`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/reply/{replyId}/message"
      if (!missing(`reply_id`)) {
        local_var_url_path <- gsub("\\{replyId\\}", URLencode(as.character(`reply_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDiscussionDiscussionReplyBundle", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param thread_id - The ID of the thread being updated
    #' @param org_sagebionetworks_repo_model_discussion_update_thread_message 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle
    #' @export
    putRepoV1ThreadThreadIdMessage = function(thread_id, org_sagebionetworks_repo_model_discussion_update_thread_message, data_file = NULL, ...) {
      local_var_response <- self$putRepoV1ThreadThreadIdMessageWithHttpInfo(thread_id, org_sagebionetworks_repo_model_discussion_update_thread_message, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param thread_id - The ID of the thread being updated
    #' @param org_sagebionetworks_repo_model_discussion_update_thread_message 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1ThreadThreadIdMessageWithHttpInfo = function(thread_id, org_sagebionetworks_repo_model_discussion_update_thread_message, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_discussion_update_thread_message`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_discussion_update_thread_message`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_discussion_update_thread_message`)) {
        local_var_body <- `org_sagebionetworks_repo_model_discussion_update_thread_message`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/thread/{threadId}/message"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{threadId\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param thread_id - the ID of the thread being marked as pinned
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    putRepoV1ThreadThreadIdPin = function(thread_id, ...) {
      local_var_response <- self$putRepoV1ThreadThreadIdPinWithHttpInfo(thread_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param thread_id - the ID of the thread being marked as pinned
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1ThreadThreadIdPinWithHttpInfo = function(thread_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }


      local_var_url_path <- "/repo/v1/thread/{threadId}/pin"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{threadId\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param thread_id - the ID of the thread that was marked as deleted
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    putRepoV1ThreadThreadIdRestore = function(thread_id, ...) {
      local_var_response <- self$putRepoV1ThreadThreadIdRestoreWithHttpInfo(thread_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param thread_id - the ID of the thread that was marked as deleted
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1ThreadThreadIdRestoreWithHttpInfo = function(thread_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }


      local_var_url_path <- "/repo/v1/thread/{threadId}/restore"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{threadId\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param thread_id - The ID of the thread being updated
    #' @param org_sagebionetworks_repo_model_discussion_update_thread_title 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle
    #' @export
    putRepoV1ThreadThreadIdTitle = function(thread_id, org_sagebionetworks_repo_model_discussion_update_thread_title, data_file = NULL, ...) {
      local_var_response <- self$putRepoV1ThreadThreadIdTitleWithHttpInfo(thread_id, org_sagebionetworks_repo_model_discussion_update_thread_title, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param thread_id - The ID of the thread being updated
    #' @param org_sagebionetworks_repo_model_discussion_update_thread_title 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1ThreadThreadIdTitleWithHttpInfo = function(thread_id, org_sagebionetworks_repo_model_discussion_update_thread_title, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_discussion_update_thread_title`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_discussion_update_thread_title`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_discussion_update_thread_title`)) {
        local_var_body <- `org_sagebionetworks_repo_model_discussion_update_thread_title`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/thread/{threadId}/title"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{threadId\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDiscussionDiscussionThreadBundle", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param thread_id - the ID of the thread being unpinned
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    putRepoV1ThreadThreadIdUnpin = function(thread_id, ...) {
      local_var_response <- self$putRepoV1ThreadThreadIdUnpinWithHttpInfo(thread_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param thread_id - the ID of the thread being unpinned
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1ThreadThreadIdUnpinWithHttpInfo = function(thread_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`thread_id`)) {
        stop("Missing required parameter `thread_id`.")
      }


      local_var_url_path <- "/repo/v1/thread/{threadId}/unpin"
      if (!missing(`thread_id`)) {
        local_var_url_path <- gsub("\\{threadId\\}", URLencode(as.character(`thread_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
