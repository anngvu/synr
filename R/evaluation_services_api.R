#' Synapse REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title EvaluationServices operations
#' @description EvaluationServicesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ deleteRepoV1EvaluationEvalId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#'
#'
#' \item status code : 204 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ deleteRepoV1EvaluationEvalIdRoundRoundId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @param } round_id character
#'
#'
#' \item status code : 204 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ deleteRepoV1EvaluationSubmissionSubId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } sub_id character
#'
#'
#' \item status code : 204 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EntityIdEvaluation } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @param } access_type \link{OrgSagebionetworksRepoModelACCESSTYPE}
#' \item \emph{ @param } active_only character
#' \item \emph{ @param } evaluation_ids character
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfEvaluation} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfEvaluation
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1Evaluation } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } access_type \link{OrgSagebionetworksRepoModelACCESSTYPE}
#' \item \emph{ @param } active_only character
#' \item \emph{ @param } evaluation_ids character
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfEvaluation} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfEvaluation
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationAvailable } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } active_only character
#' \item \emph{ @param } evaluation_ids character
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfEvaluation} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfEvaluation
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationEvalId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelEvaluation} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksEvaluationModelEvaluation
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationEvalIdAccess } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @param } access_type character
#'
#'
#' \item status code : 200 | the access types that the given user has to the given resource
#'
#' \item return type : character
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationEvalIdAcl } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelAccessControlList} \cr
#'
#'
#' \item status code : 200 | The ACL requested.
#'
#' \item return type : OrgSagebionetworksRepoModelAccessControlList
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationEvalIdPermissions } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelUserEvaluationPermissions} \cr
#'
#'
#' \item status code : 200 | The requested user permissions.
#'
#' \item return type : OrgSagebionetworksEvaluationModelUserEvaluationPermissions
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationEvalIdRoundRoundId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @param } round_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelEvaluationRound} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksEvaluationModelEvaluationRound
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationEvalIdSubmission } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfSubmission} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfSubmission
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationEvalIdSubmissionAll } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @param } status character
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfSubmission} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfSubmission
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationEvalIdSubmissionBundle } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfSubmissionBundle} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfSubmissionBundle
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationEvalIdSubmissionBundleAll } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @param } status character
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfSubmissionBundle} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfSubmissionBundle
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationEvalIdSubmissionCount } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : numeric
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationEvalIdSubmissionStatusAll } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @param } status character
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfSubmissionStatus} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfSubmissionStatus
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationEvalIdTeamIdSubmissionEligibility } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelTeamSubmissionEligibility} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksEvaluationModelTeamSubmissionEligibility
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationNameName } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } name character
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelEvaluation} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksEvaluationModelEvaluation
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationSubmissionSubId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } sub_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelSubmission} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksEvaluationModelSubmission
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationSubmissionSubIdFileFileHandleId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } sub_id character
#' \item \emph{ @param } file_handle_id character
#' \item \emph{ @param } redirect character
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ getRepoV1EvaluationSubmissionSubIdStatus } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } sub_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelSubmissionStatus} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksEvaluationModelSubmissionStatus
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1AdminEvaluationSubmissionSubIdContributor } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } sub_id character
#' \item \emph{ @param } org_sagebionetworks_evaluation_model_submission_contributor \link{OrgSagebionetworksEvaluationModelSubmissionContributor}
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelSubmissionContributor} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksEvaluationModelSubmissionContributor
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1Evaluation } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_evaluation_model_evaluation \link{OrgSagebionetworksEvaluationModelEvaluation}
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelEvaluation} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksEvaluationModelEvaluation
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1EvaluationEvalIdMigratequota } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#'
#'
#' \item status code : 200 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1EvaluationEvalIdRound } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @param } org_sagebionetworks_evaluation_model_evaluation_round \link{OrgSagebionetworksEvaluationModelEvaluationRound}
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelEvaluationRound} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksEvaluationModelEvaluationRound
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1EvaluationEvalIdRoundList } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @param } org_sagebionetworks_evaluation_model_evaluation_round_list_request \link{OrgSagebionetworksEvaluationModelEvaluationRoundListRequest}
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelEvaluationRoundListResponse} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksEvaluationModelEvaluationRoundListResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ postRepoV1EvaluationSubmission } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } challenge_endpoint character
#' \item \emph{ @param } notification_unsubscribe_endpoint character
#' \item \emph{ @param } org_sagebionetworks_evaluation_model_submission \link{OrgSagebionetworksEvaluationModelSubmission}
#' \item \emph{ @param } etag character
#' \item \emph{ @param } submission_eligibility_hash character
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelSubmission} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksEvaluationModelSubmission
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1EvaluationAcl } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_access_control_list \link{OrgSagebionetworksRepoModelAccessControlList}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelAccessControlList} \cr
#'
#'
#' \item status code : 200 | The updated ACL.
#'
#' \item return type : OrgSagebionetworksRepoModelAccessControlList
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1EvaluationEvalId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @param } org_sagebionetworks_evaluation_model_evaluation \link{OrgSagebionetworksEvaluationModelEvaluation}
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelEvaluation} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksEvaluationModelEvaluation
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1EvaluationEvalIdRoundRoundId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @param } round_id character
#' \item \emph{ @param } org_sagebionetworks_evaluation_model_evaluation_round \link{OrgSagebionetworksEvaluationModelEvaluationRound}
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelEvaluationRound} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksEvaluationModelEvaluationRound
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1EvaluationEvalIdStatusBatch } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } eval_id character
#' \item \emph{ @param } org_sagebionetworks_evaluation_model_submission_status_batch \link{OrgSagebionetworksEvaluationModelSubmissionStatusBatch}
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelBatchUploadResponse} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksEvaluationModelBatchUploadResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1EvaluationSubmissionSubIdCancellation } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } sub_id character
#'
#'
#' \item status code : 204 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ putRepoV1EvaluationSubmissionSubIdStatus } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } sub_id character
#' \item \emph{ @param } org_sagebionetworks_evaluation_model_submission_status \link{OrgSagebionetworksEvaluationModelSubmissionStatus}
#' \item \emph{ @returnType } \link{OrgSagebionetworksEvaluationModelSubmissionStatus} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksEvaluationModelSubmissionStatus
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  deleteRepoV1EvaluationEvalId  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | - the ID of the requested Evaluation
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$deleteRepoV1EvaluationEvalId(var_eval_id)
#'
#'
#' ####################  deleteRepoV1EvaluationEvalIdRoundRoundId  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | 
#' var_round_id <- "round_id_example" # character | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$deleteRepoV1EvaluationEvalIdRoundRoundId(var_eval_id, var_round_id)
#'
#'
#' ####################  deleteRepoV1EvaluationSubmissionSubId  ####################
#'
#' library(synr)
#' var_sub_id <- "sub_id_example" # character | - the ID of the Submission to be deleted.
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$deleteRepoV1EvaluationSubmissionSubId(var_sub_id)
#'
#'
#' ####################  getRepoV1EntityIdEvaluation  ####################
#'
#' library(synr)
#' var_id <- "id_example" # character | the ID of the project
#' var_access_type <- org.sagebionetworks.repo.model.ACCESS_TYPE$new() # OrgSagebionetworksRepoModelACCESSTYPE | The type of access for the user to filter for, optional and defaults to ,<a href=\"${org.sagebionetworks.repo.model.ACCESS_TYPE}\">,ACCESS_TYPE.READ,</a> (Optional)
#' var_active_only <- "active_only_example" # character | If 'true' then return only those evaluations with rounds defined and for which the current time is in one of the rounds. (Optional)
#' var_evaluation_ids <- "evaluation_ids_example" # character | an optional, comma-delimited list of evaluation IDs to which the response is limited (Optional)
#' var_offset <- 3.4 # numeric | The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0. (Optional)
#' var_limit <- 3.4 # numeric | Limits the number of entities that will be fetched for this             page. When null it will default to 10. (Optional)
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EntityIdEvaluation(var_id, access_type = var_access_type, active_only = var_active_only, evaluation_ids = var_evaluation_ids, offset = var_offset, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$getRepoV1EntityIdEvaluation(var_id, access_type = var_access_type, active_only = var_active_only, evaluation_ids = var_evaluation_ids, offset = var_offset, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  getRepoV1Evaluation  ####################
#'
#' library(synr)
#' var_access_type <- org.sagebionetworks.repo.model.ACCESS_TYPE$new() # OrgSagebionetworksRepoModelACCESSTYPE | The type of access for the user to filter for, optional and defaults to ,<a href=\"${org.sagebionetworks.repo.model.ACCESS_TYPE}\">,ACCESS_TYPE.READ,</a> (Optional)
#' var_active_only <- "active_only_example" # character | If 'true' then return only those evaluations with rounds defined and for which the current time is in one of the rounds. (Optional)
#' var_evaluation_ids <- "evaluation_ids_example" # character | an optional, comma-delimited list of evaluation IDs to which the response is limited (Optional)
#' var_offset <- 3.4 # numeric | The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0. (Optional)
#' var_limit <- 3.4 # numeric | Limits the number of entities that will be fetched for this             page. When null it will default to 10. (Optional)
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1Evaluation(access_type = var_access_type, active_only = var_active_only, evaluation_ids = var_evaluation_ids, offset = var_offset, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$getRepoV1Evaluation(access_type = var_access_type, active_only = var_active_only, evaluation_ids = var_evaluation_ids, offset = var_offset, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationAvailable  ####################
#'
#' library(synr)
#' var_active_only <- "active_only_example" # character |  (Optional)
#' var_evaluation_ids <- "evaluation_ids_example" # character | an optional, comma-delimited list of evaluation IDs to which the response is limited (Optional)
#' var_offset <- 3.4 # numeric | The offset index determines where this page will start from.             An index of 0 is the first evaluation. When null it will default             to 0. (Optional)
#' var_limit <- 3.4 # numeric | Limits the number of entities that will be fetched for this             page. When null it will default to 10. (Optional)
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationAvailable(active_only = var_active_only, evaluation_ids = var_evaluation_ids, offset = var_offset, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationAvailable(active_only = var_active_only, evaluation_ids = var_evaluation_ids, offset = var_offset, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationEvalId  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | - the ID of the desired Evaluation
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationEvalId(var_eval_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationEvalId(var_eval_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationEvalIdAccess  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | 
#' var_access_type <- "access_type_example" # character | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationEvalIdAccess(var_eval_id, var_access_typedata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationEvalIdAccess(var_eval_id, var_access_type)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationEvalIdAcl  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | The ID of the evaluation whose ACL is being retrieved.
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationEvalIdAcl(var_eval_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationEvalIdAcl(var_eval_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationEvalIdPermissions  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | The ID of the evaluation over which the user permission are being retrieved.
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationEvalIdPermissions(var_eval_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationEvalIdPermissions(var_eval_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationEvalIdRoundRoundId  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | 
#' var_round_id <- "round_id_example" # character | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationEvalIdRoundRoundId(var_eval_id, var_round_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationEvalIdRoundRoundId(var_eval_id, var_round_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationEvalIdSubmission  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | - the ID of the specified Evaluation.
#' var_offset <- 3.4 # numeric | The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0. (Optional)
#' var_limit <- 3.4 # numeric | Limits the number of entities that will be fetched for this             page. When null it will default to 10. (Optional)
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationEvalIdSubmission(var_eval_id, offset = var_offset, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationEvalIdSubmission(var_eval_id, offset = var_offset, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationEvalIdSubmissionAll  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | - the ID of the specified Evaluation.
#' var_status <- "status_example" # character | 
#' var_offset <- 3.4 # numeric | The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0. (Optional)
#' var_limit <- 3.4 # numeric | Limits the number of entities that will be fetched for this             page. When null it will default to 10, max value 100. (Optional)
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationEvalIdSubmissionAll(var_eval_id, var_status, offset = var_offset, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationEvalIdSubmissionAll(var_eval_id, var_status, offset = var_offset, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationEvalIdSubmissionBundle  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | - the ID of the specified Evaluation.
#' var_offset <- 3.4 # numeric | The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0. (Optional)
#' var_limit <- 3.4 # numeric | Limits the number of entities that will be fetched for this             page. When null it will default to 10. (Optional)
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationEvalIdSubmissionBundle(var_eval_id, offset = var_offset, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationEvalIdSubmissionBundle(var_eval_id, offset = var_offset, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationEvalIdSubmissionBundleAll  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | - the ID of the specified Evaluation.
#' var_status <- "status_example" # character | 
#' var_offset <- 3.4 # numeric | The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0. (Optional)
#' var_limit <- 3.4 # numeric | Limits the number of entities that will be fetched for this             page. When null it will default to 10, max value 100. (Optional)
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationEvalIdSubmissionBundleAll(var_eval_id, var_status, offset = var_offset, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationEvalIdSubmissionBundleAll(var_eval_id, var_status, offset = var_offset, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationEvalIdSubmissionCount  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | - the ID of the specified Evaluation.
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationEvalIdSubmissionCount(var_eval_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationEvalIdSubmissionCount(var_eval_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationEvalIdSubmissionStatusAll  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | - the ID of the specified Evaluation.
#' var_status <- "status_example" # character | 
#' var_offset <- 3.4 # numeric | The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0. (Optional)
#' var_limit <- 3.4 # numeric | Limits the number of entities that will be fetched for this             page. When null it will default to 10, max value 100. (Optional)
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationEvalIdSubmissionStatusAll(var_eval_id, var_status, offset = var_offset, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationEvalIdSubmissionStatusAll(var_eval_id, var_status, offset = var_offset, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationEvalIdTeamIdSubmissionEligibility  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | 
#' var_id <- "id_example" # character | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationEvalIdTeamIdSubmissionEligibility(var_eval_id, var_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationEvalIdTeamIdSubmissionEligibility(var_eval_id, var_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationNameName  ####################
#'
#' library(synr)
#' var_name <- "name_example" # character | - the name of the desired Evaluation.
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationNameName(var_namedata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationNameName(var_name)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationSubmissionSubId  ####################
#'
#' library(synr)
#' var_sub_id <- "sub_id_example" # character | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationSubmissionSubId(var_sub_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationSubmissionSubId(var_sub_id)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationSubmissionSubIdFileFileHandleId  ####################
#'
#' library(synr)
#' var_sub_id <- "sub_id_example" # character | 
#' var_file_handle_id <- "file_handle_id_example" # character | - the ID of the requested FileHandle contained in the Submission.
#' var_redirect <- "redirect_example" # character |  (Optional)
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationSubmissionSubIdFileFileHandleId(var_sub_id, var_file_handle_id, redirect = var_redirectdata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationSubmissionSubIdFileFileHandleId(var_sub_id, var_file_handle_id, redirect = var_redirect)
#' dput(result)
#'
#'
#' ####################  getRepoV1EvaluationSubmissionSubIdStatus  ####################
#'
#' library(synr)
#' var_sub_id <- "sub_id_example" # character | - the ID of the requested SubmissionStatus.
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$getRepoV1EvaluationSubmissionSubIdStatus(var_sub_iddata_file = "result.txt")
#' result <- api_instance$getRepoV1EvaluationSubmissionSubIdStatus(var_sub_id)
#' dput(result)
#'
#'
#' ####################  postRepoV1AdminEvaluationSubmissionSubIdContributor  ####################
#'
#' library(synr)
#' var_sub_id <- "sub_id_example" # character | 
#' var_org_sagebionetworks_evaluation_model_submission_contributor <- org.sagebionetworks.evaluation.model.SubmissionContributor$new("principalId_example", "createdOn_example") # OrgSagebionetworksEvaluationModelSubmissionContributor | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1AdminEvaluationSubmissionSubIdContributor(var_sub_id, var_org_sagebionetworks_evaluation_model_submission_contributordata_file = "result.txt")
#' result <- api_instance$postRepoV1AdminEvaluationSubmissionSubIdContributor(var_sub_id, var_org_sagebionetworks_evaluation_model_submission_contributor)
#' dput(result)
#'
#'
#' ####################  postRepoV1Evaluation  ####################
#'
#' library(synr)
#' var_org_sagebionetworks_evaluation_model_evaluation <- org.sagebionetworks.evaluation.model.Evaluation$new("id_example", "etag_example", "name_example", "description_example", "ownerId_example", "createdOn_example", "contentSource_example", "submissionInstructionsMessage_example", "submissionReceiptMessage_example", org.sagebionetworks.evaluation.model.SubmissionQuota$new("firstRoundStart_example", 123, 123, 123)) # OrgSagebionetworksEvaluationModelEvaluation | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1Evaluation(var_org_sagebionetworks_evaluation_model_evaluationdata_file = "result.txt")
#' result <- api_instance$postRepoV1Evaluation(var_org_sagebionetworks_evaluation_model_evaluation)
#' dput(result)
#'
#'
#' ####################  postRepoV1EvaluationEvalIdMigratequota  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$postRepoV1EvaluationEvalIdMigratequota(var_eval_id)
#'
#'
#' ####################  postRepoV1EvaluationEvalIdRound  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | 
#' var_org_sagebionetworks_evaluation_model_evaluation_round <- org.sagebionetworks.evaluation.model.EvaluationRound$new("evaluationId_example", "roundStart_example", "roundEnd_example", "id_example", "etag_example", c(org.sagebionetworks.evaluation.model.EvaluationRoundLimit$new("limitType_example", 123))) # OrgSagebionetworksEvaluationModelEvaluationRound | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1EvaluationEvalIdRound(var_eval_id, var_org_sagebionetworks_evaluation_model_evaluation_rounddata_file = "result.txt")
#' result <- api_instance$postRepoV1EvaluationEvalIdRound(var_eval_id, var_org_sagebionetworks_evaluation_model_evaluation_round)
#' dput(result)
#'
#'
#' ####################  postRepoV1EvaluationEvalIdRoundList  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | 
#' var_org_sagebionetworks_evaluation_model_evaluation_round_list_request <- org.sagebionetworks.evaluation.model.EvaluationRoundListRequest$new("nextPageToken_example") # OrgSagebionetworksEvaluationModelEvaluationRoundListRequest | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1EvaluationEvalIdRoundList(var_eval_id, var_org_sagebionetworks_evaluation_model_evaluation_round_list_requestdata_file = "result.txt")
#' result <- api_instance$postRepoV1EvaluationEvalIdRoundList(var_eval_id, var_org_sagebionetworks_evaluation_model_evaluation_round_list_request)
#' dput(result)
#'
#'
#' ####################  postRepoV1EvaluationSubmission  ####################
#'
#' library(synr)
#' var_challenge_endpoint <- "challenge_endpoint_example" # character | The portal endpoint prefix to the an entity/challenge page. The entity ID of the  challenge project is appended to create the complete URL. In normal operation, this parameter should be omitted.
#' var_notification_unsubscribe_endpoint <- "notification_unsubscribe_endpoint_example" # character | The portal endpoint prefix for one-click email unsubscription.  A signed, serialized token is appended to create the complete URL:  ,<a href=\"${org.sagebionetworks.repo.model.message.NotificationSettingsSignedToken}\">,NotificationSettingsSignedToken,</a>,.  In normal operation, this parameter should be omitted.
#' var_org_sagebionetworks_evaluation_model_submission <- org.sagebionetworks.evaluation.model.Submission$new("id_example", "userId_example", "submitterAlias_example", "evaluationId_example", "evaluationRoundId_example", "entityId_example", "entityBundleJSON_example", 123, "dockerRepositoryName_example", "dockerDigest_example", "name_example", "createdOn_example", "teamId_example", c(org.sagebionetworks.evaluation.model.SubmissionContributor$new("principalId_example", "createdOn_example"))) # OrgSagebionetworksEvaluationModelSubmission | 
#' var_etag <- "etag_example" # character |  (Optional)
#' var_submission_eligibility_hash <- "submission_eligibility_hash_example" # character | The hash provided by the  ,<a href=\"${org.sagebionetworks.evaluation.model.TeamSubmissionEligibility}\">,TeamSubmissionEligibility,</a>,  object. (Optional)
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$postRepoV1EvaluationSubmission(var_challenge_endpoint, var_notification_unsubscribe_endpoint, var_org_sagebionetworks_evaluation_model_submission, etag = var_etag, submission_eligibility_hash = var_submission_eligibility_hashdata_file = "result.txt")
#' result <- api_instance$postRepoV1EvaluationSubmission(var_challenge_endpoint, var_notification_unsubscribe_endpoint, var_org_sagebionetworks_evaluation_model_submission, etag = var_etag, submission_eligibility_hash = var_submission_eligibility_hash)
#' dput(result)
#'
#'
#' ####################  putRepoV1EvaluationAcl  ####################
#'
#' library(synr)
#' var_org_sagebionetworks_repo_model_access_control_list <- org.sagebionetworks.repo.model.AccessControlList$new("id_example", "createdBy_example", "creationDate_example", "modifiedBy_example", "modifiedOn_example", "etag_example", c(org.sagebionetworks.repo.model.ResourceAccess$new(123, c("accessType_example")))) # OrgSagebionetworksRepoModelAccessControlList | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$putRepoV1EvaluationAcl(var_org_sagebionetworks_repo_model_access_control_listdata_file = "result.txt")
#' result <- api_instance$putRepoV1EvaluationAcl(var_org_sagebionetworks_repo_model_access_control_list)
#' dput(result)
#'
#'
#' ####################  putRepoV1EvaluationEvalId  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | - the ID of the Evaluation being updated
#' var_org_sagebionetworks_evaluation_model_evaluation <- org.sagebionetworks.evaluation.model.Evaluation$new("id_example", "etag_example", "name_example", "description_example", "ownerId_example", "createdOn_example", "contentSource_example", "submissionInstructionsMessage_example", "submissionReceiptMessage_example", org.sagebionetworks.evaluation.model.SubmissionQuota$new("firstRoundStart_example", 123, 123, 123)) # OrgSagebionetworksEvaluationModelEvaluation | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$putRepoV1EvaluationEvalId(var_eval_id, var_org_sagebionetworks_evaluation_model_evaluationdata_file = "result.txt")
#' result <- api_instance$putRepoV1EvaluationEvalId(var_eval_id, var_org_sagebionetworks_evaluation_model_evaluation)
#' dput(result)
#'
#'
#' ####################  putRepoV1EvaluationEvalIdRoundRoundId  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | 
#' var_round_id <- "round_id_example" # character | 
#' var_org_sagebionetworks_evaluation_model_evaluation_round <- org.sagebionetworks.evaluation.model.EvaluationRound$new("evaluationId_example", "roundStart_example", "roundEnd_example", "id_example", "etag_example", c(org.sagebionetworks.evaluation.model.EvaluationRoundLimit$new("limitType_example", 123))) # OrgSagebionetworksEvaluationModelEvaluationRound | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$putRepoV1EvaluationEvalIdRoundRoundId(var_eval_id, var_round_id, var_org_sagebionetworks_evaluation_model_evaluation_rounddata_file = "result.txt")
#' result <- api_instance$putRepoV1EvaluationEvalIdRoundRoundId(var_eval_id, var_round_id, var_org_sagebionetworks_evaluation_model_evaluation_round)
#' dput(result)
#'
#'
#' ####################  putRepoV1EvaluationEvalIdStatusBatch  ####################
#'
#' library(synr)
#' var_eval_id <- "eval_id_example" # character | the ID of the Evaluation to which the SubmissionSatus objects belong.
#' var_org_sagebionetworks_evaluation_model_submission_status_batch <- org.sagebionetworks.evaluation.model.SubmissionStatusBatch$new(c(org.sagebionetworks.evaluation.model.SubmissionStatus$new("id_example", "etag_example", "modifiedOn_example", "status_example", 123, "report_example", org.sagebionetworks.repo.model.annotation.Annotations$new("objectId_example", "scopeId_example", 123, c(org.sagebionetworks.repo.model.annotation.StringAnnotation$new("key_example", "isPrivate_example", "value_example")), c(org.sagebionetworks.repo.model.annotation.LongAnnotation$new("key_example", "isPrivate_example", 123)), c(org.sagebionetworks.repo.model.annotation.DoubleAnnotation$new("key_example", "isPrivate_example", 123))), org.sagebionetworks.repo.model.annotation.v2.Annotations$new("id_example", "etag_example", c(key = org.sagebionetworks.repo.model.annotation.v2.AnnotationsValue$new(c("value_example"), "type_example"))), "entityId_example", 123, 123, "canCancel_example", "cancelRequested_example")), "batchToken_example", "isFirstBatch_example", "isLastBatch_example") # OrgSagebionetworksEvaluationModelSubmissionStatusBatch | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$putRepoV1EvaluationEvalIdStatusBatch(var_eval_id, var_org_sagebionetworks_evaluation_model_submission_status_batchdata_file = "result.txt")
#' result <- api_instance$putRepoV1EvaluationEvalIdStatusBatch(var_eval_id, var_org_sagebionetworks_evaluation_model_submission_status_batch)
#' dput(result)
#'
#'
#' ####################  putRepoV1EvaluationSubmissionSubIdCancellation  ####################
#'
#' library(synr)
#' var_sub_id <- "sub_id_example" # character | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$putRepoV1EvaluationSubmissionSubIdCancellation(var_sub_id)
#'
#'
#' ####################  putRepoV1EvaluationSubmissionSubIdStatus  ####################
#'
#' library(synr)
#' var_sub_id <- "sub_id_example" # character | - the ID of the SubmissionStatus being updated.
#' var_org_sagebionetworks_evaluation_model_submission_status <- org.sagebionetworks.evaluation.model.SubmissionStatus$new("id_example", "etag_example", "modifiedOn_example", "status_example", 123, "report_example", org.sagebionetworks.repo.model.annotation.Annotations$new("objectId_example", "scopeId_example", 123, c(org.sagebionetworks.repo.model.annotation.StringAnnotation$new("key_example", "isPrivate_example", "value_example")), c(org.sagebionetworks.repo.model.annotation.LongAnnotation$new("key_example", "isPrivate_example", 123)), c(org.sagebionetworks.repo.model.annotation.DoubleAnnotation$new("key_example", "isPrivate_example", 123))), org.sagebionetworks.repo.model.annotation.v2.Annotations$new("id_example", "etag_example", c(key = org.sagebionetworks.repo.model.annotation.v2.AnnotationsValue$new(c("value_example"), "type_example"))), "entityId_example", 123, 123, "canCancel_example", "cancelRequested_example") # OrgSagebionetworksEvaluationModelSubmissionStatus | 
#'
#' api_instance <- EvaluationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$putRepoV1EvaluationSubmissionSubIdStatus(var_sub_id, var_org_sagebionetworks_evaluation_model_submission_statusdata_file = "result.txt")
#' result <- api_instance$putRepoV1EvaluationSubmissionSubIdStatus(var_sub_id, var_org_sagebionetworks_evaluation_model_submission_status)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
EvaluationServicesApi <- R6::R6Class(
  "EvaluationServicesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new EvaluationServicesApi.
    #'
    #' @description
    #' Initialize a new EvaluationServicesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the requested Evaluation
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    deleteRepoV1EvaluationEvalId = function(eval_id, ...) {
      local_var_response <- self$deleteRepoV1EvaluationEvalIdWithHttpInfo(eval_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the requested Evaluation
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    deleteRepoV1EvaluationEvalIdWithHttpInfo = function(eval_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }


      local_var_url_path <- "/repo/v1/evaluation/{evalId}"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param round_id 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    deleteRepoV1EvaluationEvalIdRoundRoundId = function(eval_id, round_id, ...) {
      local_var_response <- self$deleteRepoV1EvaluationEvalIdRoundRoundIdWithHttpInfo(eval_id, round_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param round_id 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    deleteRepoV1EvaluationEvalIdRoundRoundIdWithHttpInfo = function(eval_id, round_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }

      if (missing(`round_id`)) {
        stop("Missing required parameter `round_id`.")
      }



      local_var_url_path <- "/repo/v1/evaluation/{evalId}/round/{roundId}"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`round_id`)) {
        local_var_url_path <- gsub("\\{roundId\\}", URLencode(as.character(`round_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param sub_id - the ID of the Submission to be deleted.
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    deleteRepoV1EvaluationSubmissionSubId = function(sub_id, ...) {
      local_var_response <- self$deleteRepoV1EvaluationSubmissionSubIdWithHttpInfo(sub_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param sub_id - the ID of the Submission to be deleted.
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    deleteRepoV1EvaluationSubmissionSubIdWithHttpInfo = function(sub_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`sub_id`)) {
        stop("Missing required parameter `sub_id`.")
      }


      local_var_url_path <- "/repo/v1/evaluation/submission/{subId}"
      if (!missing(`sub_id`)) {
        local_var_url_path <- gsub("\\{subId\\}", URLencode(as.character(`sub_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id the ID of the project
    #' @param access_type (optional) The type of access for the user to filter for, optional and defaults to ,<a href=\"${org.sagebionetworks.repo.model.ACCESS_TYPE}\">,ACCESS_TYPE.READ,</a>
    #' @param active_only (optional) If 'true' then return only those evaluations with rounds defined and for which the current time is in one of the rounds.
    #' @param evaluation_ids (optional) an optional, comma-delimited list of evaluation IDs to which the response is limited
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfEvaluation
    #' @export
    getRepoV1EntityIdEvaluation = function(id, access_type = NULL, active_only = NULL, evaluation_ids = NULL, offset = NULL, limit = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EntityIdEvaluationWithHttpInfo(id, access_type, active_only, evaluation_ids, offset, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id the ID of the project
    #' @param access_type (optional) The type of access for the user to filter for, optional and defaults to ,<a href=\"${org.sagebionetworks.repo.model.ACCESS_TYPE}\">,ACCESS_TYPE.READ,</a>
    #' @param active_only (optional) If 'true' then return only those evaluations with rounds defined and for which the current time is in one of the rounds.
    #' @param evaluation_ids (optional) an optional, comma-delimited list of evaluation IDs to which the response is limited
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfEvaluation) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EntityIdEvaluationWithHttpInfo = function(id, access_type = NULL, active_only = NULL, evaluation_ids = NULL, offset = NULL, limit = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }







      query_params[["accessType"]] <- `access_type`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["evaluationIds"]] <- `evaluation_ids`

      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/repo/v1/entity/{id}/evaluation"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfEvaluation", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param access_type (optional) The type of access for the user to filter for, optional and defaults to ,<a href=\"${org.sagebionetworks.repo.model.ACCESS_TYPE}\">,ACCESS_TYPE.READ,</a>
    #' @param active_only (optional) If 'true' then return only those evaluations with rounds defined and for which the current time is in one of the rounds.
    #' @param evaluation_ids (optional) an optional, comma-delimited list of evaluation IDs to which the response is limited
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfEvaluation
    #' @export
    getRepoV1Evaluation = function(access_type = NULL, active_only = NULL, evaluation_ids = NULL, offset = NULL, limit = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationWithHttpInfo(access_type, active_only, evaluation_ids, offset, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param access_type (optional) The type of access for the user to filter for, optional and defaults to ,<a href=\"${org.sagebionetworks.repo.model.ACCESS_TYPE}\">,ACCESS_TYPE.READ,</a>
    #' @param active_only (optional) If 'true' then return only those evaluations with rounds defined and for which the current time is in one of the rounds.
    #' @param evaluation_ids (optional) an optional, comma-delimited list of evaluation IDs to which the response is limited
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfEvaluation) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationWithHttpInfo = function(access_type = NULL, active_only = NULL, evaluation_ids = NULL, offset = NULL, limit = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      query_params[["accessType"]] <- `access_type`

      query_params[["activeOnly"]] <- `active_only`

      query_params[["evaluationIds"]] <- `evaluation_ids`

      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/repo/v1/evaluation"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfEvaluation", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param active_only (optional) No description
    #' @param evaluation_ids (optional) an optional, comma-delimited list of evaluation IDs to which the response is limited
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first evaluation. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfEvaluation
    #' @export
    getRepoV1EvaluationAvailable = function(active_only = NULL, evaluation_ids = NULL, offset = NULL, limit = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationAvailableWithHttpInfo(active_only, evaluation_ids, offset, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param active_only (optional) No description
    #' @param evaluation_ids (optional) an optional, comma-delimited list of evaluation IDs to which the response is limited
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first evaluation. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfEvaluation) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationAvailableWithHttpInfo = function(active_only = NULL, evaluation_ids = NULL, offset = NULL, limit = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      query_params[["activeOnly"]] <- `active_only`

      query_params[["evaluationIds"]] <- `evaluation_ids`

      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/repo/v1/evaluation/available"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfEvaluation", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the desired Evaluation
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelEvaluation
    #' @export
    getRepoV1EvaluationEvalId = function(eval_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationEvalIdWithHttpInfo(eval_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the desired Evaluation
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelEvaluation) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationEvalIdWithHttpInfo = function(eval_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }


      local_var_url_path <- "/repo/v1/evaluation/{evalId}"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelEvaluation", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param access_type 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return character
    #' @export
    getRepoV1EvaluationEvalIdAccess = function(eval_id, access_type, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationEvalIdAccessWithHttpInfo(eval_id, access_type, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param access_type 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (character) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationEvalIdAccessWithHttpInfo = function(eval_id, access_type, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }

      if (missing(`access_type`)) {
        stop("Missing required parameter `access_type`.")
      }



      query_params[["accessType"]] <- `access_type`

      local_var_url_path <- "/repo/v1/evaluation/{evalId}/access"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "character", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id The ID of the evaluation whose ACL is being retrieved.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelAccessControlList
    #' @export
    getRepoV1EvaluationEvalIdAcl = function(eval_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationEvalIdAclWithHttpInfo(eval_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id The ID of the evaluation whose ACL is being retrieved.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelAccessControlList) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationEvalIdAclWithHttpInfo = function(eval_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }


      local_var_url_path <- "/repo/v1/evaluation/{evalId}/acl"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelAccessControlList", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id The ID of the evaluation over which the user permission are being retrieved.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelUserEvaluationPermissions
    #' @export
    getRepoV1EvaluationEvalIdPermissions = function(eval_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationEvalIdPermissionsWithHttpInfo(eval_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id The ID of the evaluation over which the user permission are being retrieved.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelUserEvaluationPermissions) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationEvalIdPermissionsWithHttpInfo = function(eval_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }


      local_var_url_path <- "/repo/v1/evaluation/{evalId}/permissions"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelUserEvaluationPermissions", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param round_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelEvaluationRound
    #' @export
    getRepoV1EvaluationEvalIdRoundRoundId = function(eval_id, round_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationEvalIdRoundRoundIdWithHttpInfo(eval_id, round_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param round_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelEvaluationRound) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationEvalIdRoundRoundIdWithHttpInfo = function(eval_id, round_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }

      if (missing(`round_id`)) {
        stop("Missing required parameter `round_id`.")
      }



      local_var_url_path <- "/repo/v1/evaluation/{evalId}/round/{roundId}"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`round_id`)) {
        local_var_url_path <- gsub("\\{roundId\\}", URLencode(as.character(`round_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelEvaluationRound", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the specified Evaluation.
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfSubmission
    #' @export
    getRepoV1EvaluationEvalIdSubmission = function(eval_id, offset = NULL, limit = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationEvalIdSubmissionWithHttpInfo(eval_id, offset, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the specified Evaluation.
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfSubmission) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationEvalIdSubmissionWithHttpInfo = function(eval_id, offset = NULL, limit = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }




      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/repo/v1/evaluation/{evalId}/submission"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfSubmission", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the specified Evaluation.
    #' @param status 
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10, max value 100.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfSubmission
    #' @export
    getRepoV1EvaluationEvalIdSubmissionAll = function(eval_id, status, offset = NULL, limit = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationEvalIdSubmissionAllWithHttpInfo(eval_id, status, offset, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the specified Evaluation.
    #' @param status 
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10, max value 100.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfSubmission) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationEvalIdSubmissionAllWithHttpInfo = function(eval_id, status, offset = NULL, limit = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }

      if (missing(`status`)) {
        stop("Missing required parameter `status`.")
      }





      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      query_params[["status"]] <- `status`

      local_var_url_path <- "/repo/v1/evaluation/{evalId}/submission/all"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfSubmission", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the specified Evaluation.
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfSubmissionBundle
    #' @export
    getRepoV1EvaluationEvalIdSubmissionBundle = function(eval_id, offset = NULL, limit = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationEvalIdSubmissionBundleWithHttpInfo(eval_id, offset, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the specified Evaluation.
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfSubmissionBundle) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationEvalIdSubmissionBundleWithHttpInfo = function(eval_id, offset = NULL, limit = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }




      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/repo/v1/evaluation/{evalId}/submission/bundle"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfSubmissionBundle", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the specified Evaluation.
    #' @param status 
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10, max value 100.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfSubmissionBundle
    #' @export
    getRepoV1EvaluationEvalIdSubmissionBundleAll = function(eval_id, status, offset = NULL, limit = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationEvalIdSubmissionBundleAllWithHttpInfo(eval_id, status, offset, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the specified Evaluation.
    #' @param status 
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10, max value 100.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfSubmissionBundle) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationEvalIdSubmissionBundleAllWithHttpInfo = function(eval_id, status, offset = NULL, limit = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }

      if (missing(`status`)) {
        stop("Missing required parameter `status`.")
      }





      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      query_params[["status"]] <- `status`

      local_var_url_path <- "/repo/v1/evaluation/{evalId}/submission/bundle/all"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfSubmissionBundle", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the specified Evaluation.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return numeric
    #' @export
    getRepoV1EvaluationEvalIdSubmissionCount = function(eval_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationEvalIdSubmissionCountWithHttpInfo(eval_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the specified Evaluation.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (numeric) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationEvalIdSubmissionCountWithHttpInfo = function(eval_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }


      local_var_url_path <- "/repo/v1/evaluation/{evalId}/submission/count"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "numeric", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the specified Evaluation.
    #' @param status 
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10, max value 100.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfSubmissionStatus
    #' @export
    getRepoV1EvaluationEvalIdSubmissionStatusAll = function(eval_id, status, offset = NULL, limit = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationEvalIdSubmissionStatusAllWithHttpInfo(eval_id, status, offset, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the specified Evaluation.
    #' @param status 
    #' @param offset (optional) The offset index determines where this page will start from.             An index of 0 is the first entity. When null it will default             to 0.
    #' @param limit (optional) Limits the number of entities that will be fetched for this             page. When null it will default to 10, max value 100.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfSubmissionStatus) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationEvalIdSubmissionStatusAllWithHttpInfo = function(eval_id, status, offset = NULL, limit = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }

      if (missing(`status`)) {
        stop("Missing required parameter `status`.")
      }





      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      query_params[["status"]] <- `status`

      local_var_url_path <- "/repo/v1/evaluation/{evalId}/submission/status/all"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfSubmissionStatus", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelTeamSubmissionEligibility
    #' @export
    getRepoV1EvaluationEvalIdTeamIdSubmissionEligibility = function(eval_id, id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationEvalIdTeamIdSubmissionEligibilityWithHttpInfo(eval_id, id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelTeamSubmissionEligibility) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationEvalIdTeamIdSubmissionEligibilityWithHttpInfo = function(eval_id, id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }



      local_var_url_path <- "/repo/v1/evaluation/{evalId}/team/{id}/submissionEligibility"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelTeamSubmissionEligibility", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param name - the name of the desired Evaluation.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelEvaluation
    #' @export
    getRepoV1EvaluationNameName = function(name, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationNameNameWithHttpInfo(name, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param name - the name of the desired Evaluation.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelEvaluation) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationNameNameWithHttpInfo = function(name, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }


      local_var_url_path <- "/repo/v1/evaluation/name/{name}"
      if (!missing(`name`)) {
        local_var_url_path <- gsub("\\{name\\}", URLencode(as.character(`name`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelEvaluation", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param sub_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelSubmission
    #' @export
    getRepoV1EvaluationSubmissionSubId = function(sub_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationSubmissionSubIdWithHttpInfo(sub_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param sub_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelSubmission) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationSubmissionSubIdWithHttpInfo = function(sub_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`sub_id`)) {
        stop("Missing required parameter `sub_id`.")
      }


      local_var_url_path <- "/repo/v1/evaluation/submission/{subId}"
      if (!missing(`sub_id`)) {
        local_var_url_path <- gsub("\\{subId\\}", URLencode(as.character(`sub_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelSubmission", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param sub_id 
    #' @param file_handle_id - the ID of the requested FileHandle contained in the Submission.
    #' @param redirect (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    getRepoV1EvaluationSubmissionSubIdFileFileHandleId = function(sub_id, file_handle_id, redirect = NULL, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationSubmissionSubIdFileFileHandleIdWithHttpInfo(sub_id, file_handle_id, redirect, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param sub_id 
    #' @param file_handle_id - the ID of the requested FileHandle contained in the Submission.
    #' @param redirect (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationSubmissionSubIdFileFileHandleIdWithHttpInfo = function(sub_id, file_handle_id, redirect = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`sub_id`)) {
        stop("Missing required parameter `sub_id`.")
      }

      if (missing(`file_handle_id`)) {
        stop("Missing required parameter `file_handle_id`.")
      }




      query_params[["redirect"]] <- `redirect`

      local_var_url_path <- "/repo/v1/evaluation/submission/{subId}/file/{fileHandleId}"
      if (!missing(`sub_id`)) {
        local_var_url_path <- gsub("\\{subId\\}", URLencode(as.character(`sub_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`file_handle_id`)) {
        local_var_url_path <- gsub("\\{fileHandleId\\}", URLencode(as.character(`file_handle_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param sub_id - the ID of the requested SubmissionStatus.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelSubmissionStatus
    #' @export
    getRepoV1EvaluationSubmissionSubIdStatus = function(sub_id, data_file = NULL, ...) {
      local_var_response <- self$getRepoV1EvaluationSubmissionSubIdStatusWithHttpInfo(sub_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param sub_id - the ID of the requested SubmissionStatus.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelSubmissionStatus) with additional information such as HTTP status code, headers
    #' @export
    getRepoV1EvaluationSubmissionSubIdStatusWithHttpInfo = function(sub_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`sub_id`)) {
        stop("Missing required parameter `sub_id`.")
      }


      local_var_url_path <- "/repo/v1/evaluation/submission/{subId}/status"
      if (!missing(`sub_id`)) {
        local_var_url_path <- gsub("\\{subId\\}", URLencode(as.character(`sub_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelSubmissionStatus", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param sub_id 
    #' @param org_sagebionetworks_evaluation_model_submission_contributor 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelSubmissionContributor
    #' @export
    postRepoV1AdminEvaluationSubmissionSubIdContributor = function(sub_id, org_sagebionetworks_evaluation_model_submission_contributor, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1AdminEvaluationSubmissionSubIdContributorWithHttpInfo(sub_id, org_sagebionetworks_evaluation_model_submission_contributor, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param sub_id 
    #' @param org_sagebionetworks_evaluation_model_submission_contributor 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelSubmissionContributor) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1AdminEvaluationSubmissionSubIdContributorWithHttpInfo = function(sub_id, org_sagebionetworks_evaluation_model_submission_contributor, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`sub_id`)) {
        stop("Missing required parameter `sub_id`.")
      }

      if (missing(`org_sagebionetworks_evaluation_model_submission_contributor`)) {
        stop("Missing required parameter `org_sagebionetworks_evaluation_model_submission_contributor`.")
      }



      if (!is.null(`org_sagebionetworks_evaluation_model_submission_contributor`)) {
        local_var_body <- `org_sagebionetworks_evaluation_model_submission_contributor`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/admin/evaluation/submission/{subId}/contributor"
      if (!missing(`sub_id`)) {
        local_var_url_path <- gsub("\\{subId\\}", URLencode(as.character(`sub_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelSubmissionContributor", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_evaluation_model_evaluation 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelEvaluation
    #' @export
    postRepoV1Evaluation = function(org_sagebionetworks_evaluation_model_evaluation, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1EvaluationWithHttpInfo(org_sagebionetworks_evaluation_model_evaluation, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_evaluation_model_evaluation 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelEvaluation) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1EvaluationWithHttpInfo = function(org_sagebionetworks_evaluation_model_evaluation, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_evaluation_model_evaluation`)) {
        stop("Missing required parameter `org_sagebionetworks_evaluation_model_evaluation`.")
      }


      if (!is.null(`org_sagebionetworks_evaluation_model_evaluation`)) {
        local_var_body <- `org_sagebionetworks_evaluation_model_evaluation`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/evaluation"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelEvaluation", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    postRepoV1EvaluationEvalIdMigratequota = function(eval_id, ...) {
      local_var_response <- self$postRepoV1EvaluationEvalIdMigratequotaWithHttpInfo(eval_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1EvaluationEvalIdMigratequotaWithHttpInfo = function(eval_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }


      local_var_url_path <- "/repo/v1/evaluation/{evalId}/migratequota"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param org_sagebionetworks_evaluation_model_evaluation_round 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelEvaluationRound
    #' @export
    postRepoV1EvaluationEvalIdRound = function(eval_id, org_sagebionetworks_evaluation_model_evaluation_round, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1EvaluationEvalIdRoundWithHttpInfo(eval_id, org_sagebionetworks_evaluation_model_evaluation_round, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param org_sagebionetworks_evaluation_model_evaluation_round 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelEvaluationRound) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1EvaluationEvalIdRoundWithHttpInfo = function(eval_id, org_sagebionetworks_evaluation_model_evaluation_round, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }

      if (missing(`org_sagebionetworks_evaluation_model_evaluation_round`)) {
        stop("Missing required parameter `org_sagebionetworks_evaluation_model_evaluation_round`.")
      }



      if (!is.null(`org_sagebionetworks_evaluation_model_evaluation_round`)) {
        local_var_body <- `org_sagebionetworks_evaluation_model_evaluation_round`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/evaluation/{evalId}/round"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelEvaluationRound", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param org_sagebionetworks_evaluation_model_evaluation_round_list_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelEvaluationRoundListResponse
    #' @export
    postRepoV1EvaluationEvalIdRoundList = function(eval_id, org_sagebionetworks_evaluation_model_evaluation_round_list_request, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1EvaluationEvalIdRoundListWithHttpInfo(eval_id, org_sagebionetworks_evaluation_model_evaluation_round_list_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param org_sagebionetworks_evaluation_model_evaluation_round_list_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelEvaluationRoundListResponse) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1EvaluationEvalIdRoundListWithHttpInfo = function(eval_id, org_sagebionetworks_evaluation_model_evaluation_round_list_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }

      if (missing(`org_sagebionetworks_evaluation_model_evaluation_round_list_request`)) {
        stop("Missing required parameter `org_sagebionetworks_evaluation_model_evaluation_round_list_request`.")
      }



      if (!is.null(`org_sagebionetworks_evaluation_model_evaluation_round_list_request`)) {
        local_var_body <- `org_sagebionetworks_evaluation_model_evaluation_round_list_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/evaluation/{evalId}/round/list"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelEvaluationRoundListResponse", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param challenge_endpoint The portal endpoint prefix to the an entity/challenge page. The entity ID of the  challenge project is appended to create the complete URL. In normal operation, this parameter should be omitted.
    #' @param notification_unsubscribe_endpoint The portal endpoint prefix for one-click email unsubscription.  A signed, serialized token is appended to create the complete URL:  ,<a href=\"${org.sagebionetworks.repo.model.message.NotificationSettingsSignedToken}\">,NotificationSettingsSignedToken,</a>,.  In normal operation, this parameter should be omitted.
    #' @param org_sagebionetworks_evaluation_model_submission 
    #' @param etag (optional) No description
    #' @param submission_eligibility_hash (optional) The hash provided by the  ,<a href=\"${org.sagebionetworks.evaluation.model.TeamSubmissionEligibility}\">,TeamSubmissionEligibility,</a>,  object.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelSubmission
    #' @export
    postRepoV1EvaluationSubmission = function(challenge_endpoint, notification_unsubscribe_endpoint, org_sagebionetworks_evaluation_model_submission, etag = NULL, submission_eligibility_hash = NULL, data_file = NULL, ...) {
      local_var_response <- self$postRepoV1EvaluationSubmissionWithHttpInfo(challenge_endpoint, notification_unsubscribe_endpoint, org_sagebionetworks_evaluation_model_submission, etag, submission_eligibility_hash, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param challenge_endpoint The portal endpoint prefix to the an entity/challenge page. The entity ID of the  challenge project is appended to create the complete URL. In normal operation, this parameter should be omitted.
    #' @param notification_unsubscribe_endpoint The portal endpoint prefix for one-click email unsubscription.  A signed, serialized token is appended to create the complete URL:  ,<a href=\"${org.sagebionetworks.repo.model.message.NotificationSettingsSignedToken}\">,NotificationSettingsSignedToken,</a>,.  In normal operation, this parameter should be omitted.
    #' @param org_sagebionetworks_evaluation_model_submission 
    #' @param etag (optional) No description
    #' @param submission_eligibility_hash (optional) The hash provided by the  ,<a href=\"${org.sagebionetworks.evaluation.model.TeamSubmissionEligibility}\">,TeamSubmissionEligibility,</a>,  object.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelSubmission) with additional information such as HTTP status code, headers
    #' @export
    postRepoV1EvaluationSubmissionWithHttpInfo = function(challenge_endpoint, notification_unsubscribe_endpoint, org_sagebionetworks_evaluation_model_submission, etag = NULL, submission_eligibility_hash = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`challenge_endpoint`)) {
        stop("Missing required parameter `challenge_endpoint`.")
      }

      if (missing(`notification_unsubscribe_endpoint`)) {
        stop("Missing required parameter `notification_unsubscribe_endpoint`.")
      }

      if (missing(`org_sagebionetworks_evaluation_model_submission`)) {
        stop("Missing required parameter `org_sagebionetworks_evaluation_model_submission`.")
      }






      query_params[["etag"]] <- `etag`

      query_params[["submissionEligibilityHash"]] <- `submission_eligibility_hash`

      query_params[["challengeEndpoint"]] <- `challenge_endpoint`

      query_params[["notificationUnsubscribeEndpoint"]] <- `notification_unsubscribe_endpoint`

      if (!is.null(`org_sagebionetworks_evaluation_model_submission`)) {
        local_var_body <- `org_sagebionetworks_evaluation_model_submission`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/evaluation/submission"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelSubmission", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_access_control_list 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelAccessControlList
    #' @export
    putRepoV1EvaluationAcl = function(org_sagebionetworks_repo_model_access_control_list, data_file = NULL, ...) {
      local_var_response <- self$putRepoV1EvaluationAclWithHttpInfo(org_sagebionetworks_repo_model_access_control_list, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_access_control_list 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelAccessControlList) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1EvaluationAclWithHttpInfo = function(org_sagebionetworks_repo_model_access_control_list, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_access_control_list`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_access_control_list`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_access_control_list`)) {
        local_var_body <- `org_sagebionetworks_repo_model_access_control_list`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/evaluation/acl"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelAccessControlList", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the Evaluation being updated
    #' @param org_sagebionetworks_evaluation_model_evaluation 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelEvaluation
    #' @export
    putRepoV1EvaluationEvalId = function(eval_id, org_sagebionetworks_evaluation_model_evaluation, data_file = NULL, ...) {
      local_var_response <- self$putRepoV1EvaluationEvalIdWithHttpInfo(eval_id, org_sagebionetworks_evaluation_model_evaluation, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id - the ID of the Evaluation being updated
    #' @param org_sagebionetworks_evaluation_model_evaluation 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelEvaluation) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1EvaluationEvalIdWithHttpInfo = function(eval_id, org_sagebionetworks_evaluation_model_evaluation, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }

      if (missing(`org_sagebionetworks_evaluation_model_evaluation`)) {
        stop("Missing required parameter `org_sagebionetworks_evaluation_model_evaluation`.")
      }



      if (!is.null(`org_sagebionetworks_evaluation_model_evaluation`)) {
        local_var_body <- `org_sagebionetworks_evaluation_model_evaluation`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/evaluation/{evalId}"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelEvaluation", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param round_id 
    #' @param org_sagebionetworks_evaluation_model_evaluation_round 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelEvaluationRound
    #' @export
    putRepoV1EvaluationEvalIdRoundRoundId = function(eval_id, round_id, org_sagebionetworks_evaluation_model_evaluation_round, data_file = NULL, ...) {
      local_var_response <- self$putRepoV1EvaluationEvalIdRoundRoundIdWithHttpInfo(eval_id, round_id, org_sagebionetworks_evaluation_model_evaluation_round, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id 
    #' @param round_id 
    #' @param org_sagebionetworks_evaluation_model_evaluation_round 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelEvaluationRound) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1EvaluationEvalIdRoundRoundIdWithHttpInfo = function(eval_id, round_id, org_sagebionetworks_evaluation_model_evaluation_round, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }

      if (missing(`round_id`)) {
        stop("Missing required parameter `round_id`.")
      }

      if (missing(`org_sagebionetworks_evaluation_model_evaluation_round`)) {
        stop("Missing required parameter `org_sagebionetworks_evaluation_model_evaluation_round`.")
      }




      if (!is.null(`org_sagebionetworks_evaluation_model_evaluation_round`)) {
        local_var_body <- `org_sagebionetworks_evaluation_model_evaluation_round`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/evaluation/{evalId}/round/{roundId}"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`round_id`)) {
        local_var_url_path <- gsub("\\{roundId\\}", URLencode(as.character(`round_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelEvaluationRound", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id the ID of the Evaluation to which the SubmissionSatus objects belong.
    #' @param org_sagebionetworks_evaluation_model_submission_status_batch 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelBatchUploadResponse
    #' @export
    putRepoV1EvaluationEvalIdStatusBatch = function(eval_id, org_sagebionetworks_evaluation_model_submission_status_batch, data_file = NULL, ...) {
      local_var_response <- self$putRepoV1EvaluationEvalIdStatusBatchWithHttpInfo(eval_id, org_sagebionetworks_evaluation_model_submission_status_batch, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param eval_id the ID of the Evaluation to which the SubmissionSatus objects belong.
    #' @param org_sagebionetworks_evaluation_model_submission_status_batch 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelBatchUploadResponse) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1EvaluationEvalIdStatusBatchWithHttpInfo = function(eval_id, org_sagebionetworks_evaluation_model_submission_status_batch, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`eval_id`)) {
        stop("Missing required parameter `eval_id`.")
      }

      if (missing(`org_sagebionetworks_evaluation_model_submission_status_batch`)) {
        stop("Missing required parameter `org_sagebionetworks_evaluation_model_submission_status_batch`.")
      }



      if (!is.null(`org_sagebionetworks_evaluation_model_submission_status_batch`)) {
        local_var_body <- `org_sagebionetworks_evaluation_model_submission_status_batch`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/evaluation/{evalId}/statusBatch"
      if (!missing(`eval_id`)) {
        local_var_url_path <- gsub("\\{evalId\\}", URLencode(as.character(`eval_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelBatchUploadResponse", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param sub_id 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    putRepoV1EvaluationSubmissionSubIdCancellation = function(sub_id, ...) {
      local_var_response <- self$putRepoV1EvaluationSubmissionSubIdCancellationWithHttpInfo(sub_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param sub_id 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1EvaluationSubmissionSubIdCancellationWithHttpInfo = function(sub_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`sub_id`)) {
        stop("Missing required parameter `sub_id`.")
      }


      local_var_url_path <- "/repo/v1/evaluation/submission/{subId}/cancellation"
      if (!missing(`sub_id`)) {
        local_var_url_path <- gsub("\\{subId\\}", URLencode(as.character(`sub_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param sub_id - the ID of the SubmissionStatus being updated.
    #' @param org_sagebionetworks_evaluation_model_submission_status 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksEvaluationModelSubmissionStatus
    #' @export
    putRepoV1EvaluationSubmissionSubIdStatus = function(sub_id, org_sagebionetworks_evaluation_model_submission_status, data_file = NULL, ...) {
      local_var_response <- self$putRepoV1EvaluationSubmissionSubIdStatusWithHttpInfo(sub_id, org_sagebionetworks_evaluation_model_submission_status, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param sub_id - the ID of the SubmissionStatus being updated.
    #' @param org_sagebionetworks_evaluation_model_submission_status 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksEvaluationModelSubmissionStatus) with additional information such as HTTP status code, headers
    #' @export
    putRepoV1EvaluationSubmissionSubIdStatusWithHttpInfo = function(sub_id, org_sagebionetworks_evaluation_model_submission_status, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`sub_id`)) {
        stop("Missing required parameter `sub_id`.")
      }

      if (missing(`org_sagebionetworks_evaluation_model_submission_status`)) {
        stop("Missing required parameter `org_sagebionetworks_evaluation_model_submission_status`.")
      }



      if (!is.null(`org_sagebionetworks_evaluation_model_submission_status`)) {
        local_var_body <- `org_sagebionetworks_evaluation_model_submission_status`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/evaluation/submission/{subId}/status"
      if (!missing(`sub_id`)) {
        local_var_url_path <- gsub("\\{subId\\}", URLencode(as.character(`sub_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksEvaluationModelSubmissionStatus", loadNamespace("synr")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
